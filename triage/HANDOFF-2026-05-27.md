# HANDOFF — 2026-05-27

Self-contained handoff for a fresh agent. Authoritative trackers: `TODO-validation.md`
and `CHANGELOG-data.md` (newest entries at top). This file is the quick-start.

## Current state
- Corpus **19,794** opinions (`opinions.db`, ~1.1 GB, NOT git-tracked).
- Invariants **22 ok / 2 known-baseline / 0 regressed** (`.venv/bin/python -m ndcourts_mcp.invariants`).
  Known baselines: `neutral_cite_uniqueness`=0, `nd_modern_paragraph_markers` (~75 short orders).
- `detect_cite_swap` is **fully clean: CITE 0 / DOCKET 0 / BODYDUP 0** (`.venv/bin/python -m ndcourts_mcp.detect_cite_swap`).
- **Neutral-cite sequences are gap-free 1997–2026** (every year runs 1→max, no gaps, no dups).
- Use `.venv/bin/python` (jetcite editable-installed there). Always snapshot before DB writes:
  `cp opinions.db opinions.db.bak-pre-<batch>-2026-05-27`.

## Git / release
- Branch `main`, **5 commits ahead of origin** (unpushed): `be2bc5a`, `6d65dee`, `78eace8`,
  `3fdba11`, `ba6e2ee`. Push when the user OKs (public repo `jet52/ndcourts-mcp`).
- **Public release zip is STALE** — `releases/latest/download/opinions.db.zip` predates ~40
  opinions of this session's corrections (cite-swaps, recoveries, dedup, SD purge). Regenerate
  a release when the user wants the public DB current.

## What this session did (all committed)
1. **Vol 74 §10 (`8fec3a9`)** — 74 N.D. 242 was ONE opinion (*In re Adams*, Burr J.), merged
   the CL double-ingest; **all §10 bound-volume on-page order work is now complete.**
2. **Group A cite-swaps (`8fec3a9`)** — recovered 2018 ND 246 + State v. Pailing 2019 ND 283
   (rebuilt from Westlaw .docs; rows held duplicate text); Jesser got its 936 N.W.2d 102 parallel.
   **Established policy (user ruling): a corrected opinion KEEPS its original neutral cite** —
   the new-year cite stamped on a reissue (e.g. corrected-Nodak printing "2023 ND 225") is a
   clerk template artifact, not a second cite. Normalized 3 in-body phantom cites
   (Jesser 2020→2019 ND 287, Nodak 2023→2022 ND 225, Sanderson 2025→2024 ND 232).
   Nodak/Albertson/Sanderson/Bohe confirmed already-correctly-cited.
3. **Markdown-leak cite-swaps (`be2bc5a`)** — 18 rows (2000–2012) whose text leaked a nearby
   opinion while metadata stayed correct (the `# Title … <cite>` markdown block had masked it
   from the CITE detector). Rebuilt from on-disk archive HTML. **Hardened `detect_cite_swap`**
   (`_body` skips the title block; westlaw rows excluded; BODYDUP requires a suspicious member).
4. **§6 dedup + §2 single-source** — both verified **essentially complete** (the TODO's "~530"
   and "~183" were badly stale). §6: 0 actionable merges. §2: of 6,069 gap-era (1953–96)
   opinions, ~99% are two-source; only 65 single-source, all triaged.
5. **Post-1997 no-cite investigation (`6d65dee`, `78eace8`, `3fdba11`, `ba6e2ee`)** —
   107 rows lacked a `YYYY ND n` cite. Audited the per-year sequence:
   - Recovered 2 genuinely-missing opinions: **2010 ND 54** *Hoffner v. Job Service ND* and
     **2012 ND 196** *Bank of ND v. Brown* (from archive HTML); fixed **2010 ND 149** reporter
     mis-tag. Sequences now gap-free.
   - 106 no-cite "dups" → **93 merged** into cited twins (passes: HIGH 74, v2 7, v3 6, v4 5,
     Brekhus 1; each followed by `align_primary_source --apply`).
   - **2 deleted as South Dakota contamination** (*Young v. Oury* 2013 SD 7, *Thompson v. Avera*
     2013 SD 8 — mislabeled court, in via shared N.W.2d). Corpus-wide foreign scan found ONLY these 2.

## Open items (full list in TODO-validation.md; live ones highlighted)
- **11-row no-cite tail** (low value, optional): 3 ND **Court of Appeals** stubs (CA dockets —
  *Ernst v. TJON*, *In re Nb* 20040340CA, *In re Knh*; mislabeled `court`=NDSC; correctly lack a
  `YYYY ND n` cite — could fix the court field) + 8 anonymized juvenile/social-services
  memorandum stubs (Grand Forks/Cass/McKenzie Soc. Servs., State v. P.T.D./H.B., In re F.M.G.)
  whose cited twins resisted name/surname/docket/text matching — true per-case.
- **Second-source enhancement (optional):** pull older opinion PDFs from C-Track (cross-checks
  the 35 archive-HTML cite-swap rebuilds); optionally Westlaw `.docs` for the 4,713 NW2d+court-archive.
- **Diff-audit residuals (carried from TODO, NOT re-verified — counts may be stale):**
  secondary_truncated 30, primary_short 26, primary_tiny 14, genuinely_unclear 8; tighten
  `fix_archive_pairings` to require neutral-cite agreement; detach corrupted `NW/271/771.md`.
- **Missing files:** recover `1997ND5.md` (Torres), `1998ND22.md` (Schmidt).
- **§10 display:** bracket synthetic cites `[YYYY ND nnn]` in `lookup_opinion`/MCP output (not wired).
- **Specific:** syllabus-by-the-court cutoff year; Hassen v. Salem cite (50 N.D. 825 vs 813).
- Metadata: rebuild pre-1997 `judges` from `justices.py` + disqualification lines.

## Conventions / gotchas
- **Scraper misfiles by NAME — trust the cite INSIDE the document, not the filename/DB label.**
  This bit twice this session (user-supplied "Pailing" files were both Jesser; PDF named
  2024ND232 contained 2025 ND 232). Verify content before acting.
- Canonical source tree `~/refs/nd/opin` (markdown/, NW2d/, NW/, N.D./, archive/, pdfs/).
- `merge_pair` side-effect: after merging, the survivor's primary `opinion_sources` row may not
  match `opinions.source_reporter` → run `ndcourts_mcp.align_primary_source --apply` (it treats
  `opinions.source_reporter` as ground truth). This tripped invariants 4× this session; always
  run it after a merge batch.
- FK enforcement is ON via `ndcourts_mcp.db.get_connection` (a raw `sqlite3.connect` shows
  foreign_keys=0). To delete an opinion, purge ALL 13 FK-referencing tables first (see
  `delete-sd-contamination` batch).
- Row deletions / text swaps are NOT changelog-revertible → revert via the dated
  `opinions.db.bak-pre-*-2026-05-27` snapshots (~14 from today).
- CourtListener MCP is disconnected; web search + on-disk archive HTML/PDF served lookups.
