# TODO — Path to a Fully Validated Corpus

Progress map for reaching full two-source validation of every ND Supreme Court opinion 1890–present. Updated 2026-05-24.

**End goal:** a corpus accurate enough to be offered to the ND Supreme Court as the authoritative text of its opinions, or at least a reliable proxy. That bar is higher than "useful research tool" — it drives the provenance, reversibility, and verification standards below.

**ACTIVE WORKSTREAM — §10 synthetic medium-neutral cites (full detail in §10):** Corpus **20,154**; invariants **22 ok / 2 known / 0 regressed**.
- **Phase 1A DONE 2026-05-24** — all **12,604** pre-1997 opinions carry a provisional `YYYY ND nnn` synthetic cite (`reporter='ND-neutral-synthetic'`, `is_primary=0`). **Canonical re-runnable ordering tool = `triage/section10_resequence_2026-05-24.py`** (recomputes from current DB + verified `KNOWN_ORDER`; run after any pre-1997 data change). Numbers are PROVISIONAL until a publish freeze.
- **On-page order verified** for clusters in on-disk bound vols (3, 9, 14, 16, 17, 18, 19, 20, 21, 24, 27, 34, 41, 44, 47) + **64** (via HeinOnline per-case scan). **Remaining bound work: vol 74 only** (74 N.D. 242, not on disk/archive.org — needs pp.241–243; resolve one-opinion-vs-two first). **Vol 64 reordered** 2026-05-25 (Ott→5/Thorvaldson→6, `section10-resequence-2026-05-25`). **Vol 50 resolved without the volume** (lead+per-curiam → lead-first ruling). **Vols 23 & 32 need no verification** (different-date shared pages). Plus 28 NW2d-era (1953–96) member rows needing NW2d page pulls. (*White v. Lauder* vol-10 knot RESOLVED 2026-05-25.) **Source for the gap-era volumes: HeinOnline gives individual case scans** (archive.org lacks 50/64/74).
- **Lead/dependent ordering convention (ruled 2026-05-25):** for a lead opinion + its same-day dependent per curiam sharing a page, the synthetic cite orders the **LEAD first** even where the reporter prints the short companion above it (top-of-page placement = typesetting artifact, not decision sequence). The default NW-page tiebreak already does this; no `KNOWN_ORDER` flip. Encoded in the resequencer's `KNOWN_ORDER` comment.
- **`date_filed` discrepancy resolution applied** (ratified policy: court's filing date governs; §2) — 110 corrections (108 auto ≤31d + 2 verified). **93 large-gap holds REVIEWED 2026-05-25**: 23 were genuine CL errors (CL had recorded a later rehearing/denial date; true filing date confirmed by reporter-volume window + source read — batch `fix-date-holds-clerror-verified-2026-05-25`, incl. 8454 City of Casselton 1994→1981); the other 70 KEEP CL (the court-source date there is an earlier proceeding-date/artifact, or window-ambiguous). **Lesson: for >31d holds the court-source "Filed" date is NOT reliably the opinion filing date — the N.W.2d volume window is the arbiter.**
- **Open sub-items:** §6 DEFER_MODERN ~530 post-1997 pairs (CONFIDENTIAL juvenile matters — careful); Westlaw bulk-download the ~183 truly single-source 1953–1996 opinions (§2). (*White v. Lauder* 10 N.D. 400/445 knot RESOLVED 2026-05-25 — two distinct companion opinions, `fix-white-lauder-2026-05-25`.)
- **NOTE (coordination):** a parallel session has been doing 1997+ case-name/dedup work in the same `opinions.db`. Disjoint from §10 (pre-1997), but `neutral_cite_uniqueness` may read below its 258 baseline as that work resolves native dups.

**Highest-yield next steps (2026-05-18 — exhausted/superseded: Type Y sweep done-negative + **Type-Y-at-ingest now closed** (§1, `typey.py`), citation contracts applied, `align_primary_source` already LEFT JOIN, archive.ndcourts.gov re-scrape REJECTED; PDF-era second-source policy ratified §9 and the 41/41 mandatory-OCR worklist fully Westlaw-sourced):**
1. **§6 long tail** — MERGE-HOLD-LOWJAC + NEEDS_DECISION + DEFER_MULTI all FULLY adjudicated 2026-05-18. LOWJAC: 50 merged. NEEDS_DECISION: 70 pairs → 16 merged (CL+Westlaw double-ingests) + 50 confirmed keep-separate (distinct shared-page / supplemental-by-design) + 2 stale. DEFER_MULTI: 37 cites → 5 merged + Schirado distinct + rest distinct/already-resolved; 9 post-1997 clusters reclassified into DEFER_MODERN. Corpus 20,229→**20,159**. **Only §6 work remaining: the ~530 DEFER_MODERN post-1997 pairs** (touches public release + the 258 neutral_cite_uniqueness baseline; some CONFIDENTIAL juvenile matters — handle carefully, separate vetting). Plus minor cite-hygiene follow-ups (below).
2. ~~**Case-name sweep vols 1–79** (§1)~~ — **DONE 2026-05-24.** All volumes TUI'd by the user; the 505-item ambiguous residue closed via ratified pattern sweeps + a `.doc`-body-grounded adjudicator (spelling/misattribution resolved by reading the clean Westlaw body) + ADDITION sub-classification + OTHER capacity/jurisdiction mining; 104 EQUIV → keep_db. Then a corpus-wide Latin-phrase capitalization normalization (`ex rel`/`In re`/`ex parte`, 536 titles). Batches `fix-casenames-{jurisdiction,residue-patterns,adjudicate,addition,other-mined,latin-caps,latin-caps-followup}-2026-05-24`. Commits `e4d689f`, `785ef5c`.
3. **1953–1995 single-source batch picker** (§2) — rebuild without quality_score (uninformative for that era); use metadata anomalies + cited-by counts + per-decade random sampling instead.
4. **(opt) Tombstone/`merged_into` guard** (§5) — so even a deliberate full rebuild stays idempotent against §6 merges (incremental mode already protects the weekly pipeline; this is defense-in-depth). *(Scraper docket-collision "regression" closed 2026-05-18 — not reproducible; ingest OOM + dup-resurrection fixed via `--commit-every` + `--incremental`, weekly wired.)*

## Status snapshot

| Era | Opinions | Primary source | Cross-check coverage | Status |
|-----|----------|----------------|----------------------|--------|
| 1890–1924 | ~3,748 | CourtListener NW (OCR) | Westlaw vols 1–52 applied | ✓ Complete (metadata); text-merge triage pending for 455 rows |
| 1924–1938 | 1,618 | CourtListener NW (OCR) | Westlaw vols 53–68 applied | ✓ Complete (metadata) |
| 1938–1946 | 453 | CourtListener NW/NW2d (OCR) | Westlaw vols 69–74 applied | ✓ Complete (metadata) |
| 1946–1953 | 343 | CourtListener NW2d (OCR) | Westlaw vols 75–79 applied | ✓ Complete (metadata) |
| 1953–1996 | ~8,476 | CourtListener NW2d (OCR) | None — N.D. Reports series ended | ⚠ Targeted only |
| 1997–2019 | 5,978 | ndcourts.gov (court PDF → analyzer markdown) | NW2d where it exists; Westlaw for OCR-required | ✓ PDF-era policy ratified 2026-05-18 (§9): born-digital single-source OK; scanned/OCR-required Westlaw-sourced — 41/41 done |
| 2020–present | 1,542 | ndcourts.gov (court PDF → analyzer markdown) | NW2d/Westlaw where available | ✓ Same PDF-era policy applies (§9) |

Totals below are "outstanding units of review work," not opinions.

## 1 · Westlaw cross-check — bound N.D. Reports series complete

- [x] Download and process Westlaw vols 75–79 (5 volumes, 343 opinions, 1946–1953). Applied 2026-04-27 — 10 date_filed corrections, 0 author corrections, 44 case-name diffs deferred. **All 79 volumes of the bound N.D. Reports series now Quick-Checked against Westlaw (1890–1953).**
- [x] Re-run `merge_westlaw_text --apply` to incorporate vols 49–79 .doc files (2026-04-27, batch `westlaw-text-merge-2026-04-27`). 2,733 opinions migrated to Westlaw text. Idempotency bug also fixed — query now skips already-merged opinions. **5,698 stale `source_path` rows backfilled** in the same session — long-standing bug where volume-based ingest recorded the staging path instead of the final archive path; root cause fixed in `ingest_westlaw.py`.
- [x] Triage merge_westlaw_text residuals — 137 → 29 in the 2026-04-27 session. Three parser fixes (modern Westlaw format, star-pagination + `PER CURIAM` + `District Judge` author markers, widened "All Citations" search) unlocked 108 of the 110 SKIP residuals. Tool added: `merge_westlaw_text --errors-report PATH`. Current report at `triage/westlaw-merge-residuals-2026-04-27.md` with the remaining 29 categorized.
- [x] Resolve the 9 wrong-pairings cluster from the residuals (2026-04-27, batch `westlaw-merge-residuals-2026-04-27-pairings` + `westlaw-text-merge-2026-04-27-pairings`). Each of nine vol/page locations had two short opinions (lead + per curiam follow-on) sharing an `<vol> N.D. <page>` cite; ingest had paired one .doc with the wrong opinion and orphaned the other. Fix: 9 deletions + 18 insertions in `opinion_sources`; all 18 newly-paired texts merged. Surfaced two side issues, captured below.
- [x] Resolve the remaining 20 residuals (2026-04-28). 14 Type X (parser was extracting body-only instead of full bound entry) re-merged via batch `westlaw-text-remerge-2026-04-28-full-bound` after parser fix in `ingest_westlaw.py:_extract_full_bound_text`. 6 Type Y (separate supplemental publications at distinct N.W. cites) inserted as new opinion rows 20386–20391 via batch `westlaw-supplemental-publications-2026-04-28`. 1 case (Klingenstein 2958) was already correct.
- [x] **CORPUS-WIDE syllabus recovery (2026-04-28).** Re-merged 5,701 of 5,744 westlaw-paired opinions through the new full-bound-entry parser. 4,717 pre-1953 opinions now contain "Syllabus by the Court"; the 1,022 without are correctly without (memoranda, per-curiam follow-ons). Batch `westlaw-syllabus-recovery-corpus-wide-2026-04-28`, 11,402 changelog rows, ~13 min wall-clock.
- [x] **CORPUS-WIDE Type Y sweep — DONE 2026-05-17, NEGATIVE RESULT.** Built `ndcourts_mcp/sweep_type_y.py` (hardened textutil reader w/ process-group kill; classifies by body-jaccard + shared-N.D.-cite, not the naive raw cite-mismatch which over-flags OCR noise). Scanned all 6,795 westlaw-`.doc`-paired opinions: **0 `TYPE_Y`, 0 `TYPE_Y_NO_NDSHARE`**. No hidden Type Y supplemental publications remain — the 6 inserted 2026-04-28 + this session's §6 dedup/merge work (which dissolved the apparent candidates, e.g. `109 N.W.2d 249`) cleared them. The 2026-05-13 "highest-yield" premise is exhausted: no Type Y row-gain left. Report: `triage/type-y-sweep-2026-05-17.tsv`.
- [x] **37 citation-accuracy bugs from the Type Y sweep — 30 fixed, 7 flagged (2026-05-17).** Batch `fix-cite-discrepancy-2026-05-17`: **30 SAFE** N.W. parallel-cite corrections applied (authority = Westlaw "All Citations" footer; corroborated by N.D.-cite-match for all 37 + jaccard 1.00 + era-plausibility on volume errors; changelog-revertible). Invariants 0 regressed. **7 FLAGGED** (corrected cite collides with another/adjacent oid → likely shared-page/companion/lead+rehearing/CL-dup, not a typo) → `triage/cite-discrepancy-flagged-2026-05-17.md` for per-item §6-flavored review: oids 6023, 107, 190, 2167, 2795, 4008, 4062. Cosmetic lag: wrong cite may persist in `text_content` frontmatter (display-only; `citations` table is authoritative + now correct).
- [x] **Detect Type Y at ingest time — DONE 2026-05-18.** Extracted the discriminator into a shared dependency-light module `ndcourts_mcp/typey.py` (`classify(parsed, db_cites, db_text) → {kind,…}`; only `re`+`multisource_diff`, takes a parsed dict so no `ingest_westlaw` cycle). `sweep_type_y` refactored to delegate (byte-identical TSV; proven equivalent old-vs-new on 155 identical-input oids, 0 mismatches). `ingest_westlaw.process_batch` now runs `typey.classify` after `_find_db_opinion`: a true Type Y (distinct text, body-jaccard < 0.55) is **not paired** (no wrong-row source-attach/metadata corrections) and is written to `<batch_dir>/type_y_queue.txt` for `insert_supplemental_opinions` (Option A — no inline row creation in the Quick-Check tool); CITE_DISCREPANCY/MATCH/DOC_NO_NW/DB_NO_NW behave exactly as before, so non-Type-Y ingest behavior is unchanged. Regression proof: full corpus re-sweep `triage/type-y-sweep-2026-05-18.tsv` — **0 TYPE_Y / 0 TYPE_Y_NO_NDSHARE** (CITE_DISCREPANCY 35→0 is the `fix-cite-discrepancy-2026-05-17` data fix; new DB_NO_NW/DOC_NO_NW are the now-larger westlaw-paired population — the 41 PDF-era opinions with neutral-only DB cites — benign, not Type Y).
- [ ] **Pin down the "Syllabus by the Court" cutoff year.** Sampled scan shows the practice was alive 1890s–1950s (97%+ of Westlaw .docs in that range). Post-1953 the bound N.D. Reports series ended and we have very few Westlaw .docs to sample. CL's NW2d source files don't preserve syllabi (CL never did), so they're not informative. Quick test: download Westlaw .docs for ~10 opinions each from 1955, 1965, 1975, 1985, 1995 and check for "Syllabus by the Court" presence. Pinpointing the cutoff helps target the corpus-wide syllabus-recovery sweep above.
- [x] **Dedup Ott v. Kelley 4484/4485 — DONE** (verified 2026-05-25: oid 4485 merged away/deleted; 4484 *Ott v. Kelley* @ 252 N.W. 271 survives).
- [ ] **Hassen v. Salem 2946 N.D. cite conflict** (1924-03-20 rehearing). DB cites `50 N.D. 825`; Westlaw's "All Citations" footer on the .doc says `50 N.D. 813`. The Farmers Bank .doc at the same `0813-` page also cites `50 N.D. 813`, so two distinct opinions claim that page in Westlaw. Resolve by checking the bound 50 N.D. volume directly to determine the correct N.D. starting page for the rehearing.
- [x] **Case-name sweep across vols 1–79 — DONE 2026-05-24.** All volumes TUI'd; 505-item ambiguous residue closed via ratified pattern sweeps + `.doc`-body-grounded adjudicator + corpus-wide Latin-phrase capitalization normalization. Batches `fix-casenames-*-2026-05-24` (commits `e4d689f`, `785ef5c`). (Detail in the sub-bullets below + CHANGELOG-data.md.)
  - **2026-05-18 — vols 1–5 done, then a tool defect was found and fixed.** First run applied 13 changes + 120 keep-DB; audit caught **6 wrong** (`_find_db_opinion` binds a .doc to an opinion by *shared citation alone* → on pre-1953 shared-page clusters it returned a sibling's caption, e.g. oid5274 got *Globe* instead of *Kellogg*). Reverted (`fix-casenames-vol1-5-misattrib-2026-05-18`); 7 legit kept. **Tool hardened** (committed): a `_same_case` pairing guard verifies the .doc caption against the opinion's own `text_content` title (OCR-tolerant on both party sides) and diverts true mispairings to `triage/casenames-mispaired-2026-05-18.tsv` instead of offering them; normalization no longer eats abbreviation periods or footnote-star artifacts. Detail in CHANGELOG-data.md.
  - **`triage/casenames-mispaired-2026-05-18.tsv` is a new work item**: .docs whose caption diverges from the cite-matched row's text title. **The 5 vol-1–5 entries are RESOLVED 2026-05-18 via scanned bound N.W. Reporter page images** (Westlaw-pulled, image-only PDFs = independent third witness): all 5 are *single opinions*, not shared-page tangles — CL had titled by a co-party or OCR-garbled a surname. 3 case_name corrections applied (`fix-casenames-vol1-5-bound-2026-05-18`: 5152→*Sanford v. Bell*, 5182→*Carson v. Gillitt*, 5203→*State v. Hasledahl*); 5380 *Pancoast* (alias *Kent* used in body by the court's own statement) + 5242 *Coler* confirmed already-correct. **Lesson: a mispaired-flag is often just a CL co-party/OCR title, not a real tangle — the bound reporter resolves it.** For *future* mispaired entries in later volumes, pull the bound N.W. page before assuming a missing opinion.
  - **New independent-source mechanism (2026-05-18):** scanned bound N.W. Reporter page-image PDFs filed at `~/refs/nd/opin/NW/<vol>/<page>.pdf` and registered as non-primary `NW-image` `opinion_sources` rows (SCHEMA.md convention). These advance the §9 two-source/authoritative-text bar for pre-1953 opinions where CL OCR is the only text. Westlaw-delivered → excluded from public release (refs-only; DB stores a path reference). Reusable for any contested pre-1953 caption/text going forward.
  - **Vols 6–10 mispaired worked 2026-05-19** via the bound-page mechanism: 18 of 21 PDFs delivered (3 non-unique cites still pending — 73 N.W. 1102 *O'Grady*, 84 N.W. 1117 *Emmons*, 87 N.W. 1135 *White v. Lauder*); 7 case_name corrections applied (`fix-casenames-vol6-10-bound-2026-05-19`), 10 bound-validated as-is, 1 mis-attributed source removed (`fix-mispaired-nwimage-5758-2026-05-19`). Detail in CHANGELOG-data.md.
  - **Henniges v. Johnson update 2026-05-19** (REVISED — earlier "missing opinion" finding was wrong): the Westlaw `.doc` for 9 N.D. 489 / 84 N.W. 350 matches oid5758's existing text (jaccard 0.818). One opinion, not two — *Paschke* is the third-party purchaser in the body; *Johnson* is the actual defendant; CL captioned by the wrong party. Resolved (`fix-henniges-johnson-5758-2026-05-19`): case_name fixed to *Henniges v. Johnson*, `NW/84/350.pdf` re-attached as NW-image, `.doc` registered as `westlaw` source, `81 Am.St.Rep. 588` citation added.
  - **Emmons County N.W. cite cleanup DONE 2026-05-19** (`fix-emmons-nw-cite-2026-05-19`, 4 updates): 5750 Mellon@612, 5749 Robinson@613, 5748 Thistlewaite@614 → `84 N.W. 1118`; 5747 Wilson@615 → `84 N.W. 1119` (Lilly@611 was already correct at 1117). Bound page-break: 1117 = pp.608–611; 1118 = pp.612–614; 1119 = p.615.
  - **Emmons County vol-9 gap RESOLVED 2026-05-19** (`emmons-county-vol9-ingest-2026-05-19`). 9 missing memorandum decisions ingested from N.D./9/ `.docs` (already on disk from the original vol-79 Quick Check): Cranmer@608, Baker@609, Davidson@609, Cranmer@610, Ganger@610, McKenzie@611, McKenzie@612, McLean@612, Mellon@613 (oids 20489-20497). Corpus 20,159 → 20,168. Sub-cluster cite cleanup follow-up: existing rows 5747-5751 currently carry `84 N.W. 1117` but the bound `.docs` for pages ≥611 use `84 N.W. 1118 (Mem)` — low-priority data-quality fix.
  - **Vols 11–15 mispaired RESOLVED digitally 2026-05-19** (`fix-casenames-vol11-15-bound-2026-05-19`, 14 case_name fixes + 1 source-row add). 20 mispaired entries → 14 SAME-class case_name fixes via `.doc`-body jaccard adjudication (no Westlaw page-image pull needed), 3 false-positives (curly-apos / Æ ligature), 3 false-positives (cite-shared siblings — actual opinion_sources pairings were already correct). 154-item interactive review still pending the user's TUI run.
  - **Vols 16–79 mispaired RESOLVED digitally 2026-05-20** in one big batch (`fix-casenames-vol16-79-bound-2026-05-20`, 58 case_name fixes). 107 mispaired entries adjudicated via `triage/adjudicate_mispaired_2026-05-20.py` (analyzer) + `triage/apply_mispaired_2026-05-20.py` (applier). Breakdown: 59 SAME (58 applied, oid 1354 *Minneapolis Ry. Co.* deferred — user-skipped, consolidated-case rewrite), 24 FALSE_POSITIVE_NORM (curly-apos/ligature; DB already matches bound), 10 ALREADY_PAIRED (sibling .doc correctly attached to a different opinion via opinion_sources), 7 ORPHAN_SIBLING (potential corpus gaps — see below), 6 AMBIG (jaccard 0.35–0.50, defer to manual), 1 SAME_NEEDS_HUMAN (oid 4622 *Casselton Reporter* — Westlaw caption itself truncated). **2053-item interactive review queue still pending the user's TUI run across vols 16–79.**
  - **7 ORPHAN_SIBLING entries CLOSED 2026-05-20** (`fix-casenames-vol16-79-orphans-2026-05-20`, 7 opinion_sources rows): NOT corpus gaps. Each `.doc` corresponds to an opinion already in the DB under a different OID (the cite-shared sibling of the cite-matched row); jaccard `.doc` vs correct-OID `text_content` is 0.85–0.92 for all 7. Pattern matches `fix-cairncross-source-2026-05-19` (vol 11–15) — `.doc` was just never registered as an `opinion_sources` row. Source rows added as non-primary; promotion to primary deferred to a future `merge_westlaw_text` pass. Mapping: `.doc` 0372→oid 680, 0412→692, 0348→767, 0352→937, 0395→1007, 0373→1487, 0601→1641. Case-name enrichment for 3 of these applied as `fix-casenames-vol16-79-orphan-enrich-2026-05-20`: oid 680 → *Fitzmaurice v. Willis, County Com'rs*; oid 1487 → *Sundahl v. First State Bank of Edmunds*; oid 1641 → *Mercer County State Bank of Manhaven v. Hayes*.
  - **Tool refinement surfaced**: the ORPHAN_SIBLING classifier in `triage/adjudicate_mispaired_2026-05-20.py` should also fuzzy-match the parsed `.doc` case_name against `opinions.case_name` (or look up its primary cite) — that would correctly classify these as `ALREADY_PAIRED_BY_NAME` rather than `ORPHAN_SIBLING`. The AMBIG branch needs the same refinement plus an `opinion_sources` lookup (caught 4 of 6 AMBIG entries today as already-paired or already-paired-by-name).
  - **6 AMBIG entries CLOSED 2026-05-20** (`fix-casenames-vol16-79-ambig-2026-05-20`, 2 source rows + 2 new opinions): 2 ALREADY_PAIRED_BY_NAME (Kerr-v-Herred → oid 278, Kerr-v-Swanson → oid 276), 2 ALREADY_PAIRED already-primary (Bussey → 1189, Nelson@250 → 2169 — classifier blind spots, no DB change), and **2 corpus gaps ingested** (oid 20498 *State ex rel. McCue v. Great Northern Ry. Co.* @ 19 N.D. 57 1909-04-16; oid 20499 *Lammadee v. Middlewest Grain Co.* @ 44 N.D. 247 1919-06-21). Corpus 20,168 → 20,170.
  - **2 deferred items CLOSED 2026-05-20** (`fix-casenames-vol16-79-deferred-2026-05-20`): oid 1354 *Minneapolis, St. P. & S. S. M. R. Co. v. State Board of Ry. Com'rs* (bound caption adopted in place of CL's *In re Appeal of...* docket framing); oid 4622 *Casselton Reporter v. The Fargo Forum. State ex rel. Casselton Reporter v. Doherty, County Auditor* (`_parse_westlaw_doc` truncation was the apparent-truncation; reading the .doc body confirmed the full consolidated caption). Tool refinements logged: smart_titlecase `'Rs` abbreviation handling; `_parse_westlaw_doc` follow-through for multi-case captions.
  - **2,054-item ambiguous case-name diff queue (vols 16–79) auto-triaged 2026-05-20** (`fix-casenames-vol16-79-autorev-2026-05-20`, 47 UPDATEs + 694 state.json keep_db; `fix-casenames-vol16-79-detail-2026-05-20`, 702 UPDATEs). Pipeline: `triage/extract_ambig_review_2026-05-20.py` → `triage/classify_ambig_review_2026-05-20.py` → `triage/apply_ambig_review_2026-05-20.py` for the initial cut; then `triage/subclassify_adds_detail_2026-05-20.py` + user policy review on a stratified 48-row sample → `triage/apply_adds_detail_2026-05-20.py` for the LOCALITY/ROLE/PAREN sub-buckets within ACCEPT_WL_ADDS_DETAIL. Closed-out: 749 ACCEPT_WL UPDATEs applied + 694 KEEP_DB silently (state.json). **605 still pending TUI review**: 148 OTHER residue, 45 MULTICASE_MISSED (In-re-Estate framings), 17 DOCKET_METADATA, 9 LONG_PLEADING, 1 STATE_EXPANSION, plus 372 DEFER_AMBIG + 12 DEFER_MULTICASE + 1 DEFER_WL_GARBAGE from main classification. TUI command: `python -m ndcourts_mcp.review_casenames --volumes 16-79`.
  - **Methodology shift 2026-05-19**: digital-first adjudication is the better default for mispaired entries — parse the `.doc`, jaccard vs DB text, classify SAME/DIFF; page-image pulls only when ambiguous. Used for vols 11–15; should be the default for vols 16+.
  - **Tool refinements logged**: (a) `_same_case` guard should normalize curly→straight apostrophes and Æ↔ae ligatures before comparison (saves ~3 false-positives per 20-item batch). (b) `extract_diffs` should consult existing `opinion_sources` pairings when the cite is shared — if the `.doc`'s caption matches a sibling's text title, the guard's "mispaired" classification is wrong; the actual ingest got the pairing right.
  - **O'Conner/O'Connor audit RESOLVED 2026-05-19** (no further fixes warranted). 5 surviving O'Connor rows in 1893–1905; 2 bound-validated correct (different individual / different reporter convention), 1 candidate (oid5396 *Selliger* @ 67 N.W. 824 — needs a bound pull only if/when convenient). **Refined hypothesis**: yesterday's "systematic CL OCR error" framing was too strong — the bound N.W. Reporter *itself* used different spellings across volumes for what appears to be the same M. J. O'Conner (vol 68 → CONNOR; vols 69, 84 → CONNER); CL happens to match each bound's choice case-by-case. No corpus-wide sweep needed.
  - **§6 cleanup item 2026-05-19 — RESOLVED 2026-05-25, NOT a merge**: oid5834 *White v. Lauder* — the earlier "CL date-drift double-ingest / merge_pair candidate" hypothesis was WRONG. Reading bound vol 10 showed 5834 (10 N.D. 400) and 5833 (10 N.D. 445) are two distinct companion mandamus per curiams; both kept. The "1901-10-25" that made 5834 look like a date-drift dup was actually *Willard v. Monarch*'s date (next case on bound p.400), now fixed to 1901-11-21. See `fix-white-lauder-2026-05-25`.
  - **SCHEMA follow-up**: need `AmStRep` (American State Reports) value in the `citations.reporter` taxonomy. Currently using `ALR` provisionally (closest valid enum); only one row affected so far (the oid5758 add above).
  - **CL OCR pattern surfaced 2026-05-19**: M. J. O'Conner (sheriff in multiple late-1890s ND cases) is consistently "Conner" in the bound N.W. Reporter but "Connor" across CL text/metadata — systematic OCR error. 2 fixed (oids 5430, 5762); corpus-wide audit for the same person/spelling pattern would be worth running (`SELECT … WHERE case_name LIKE '%O''Connor%' AND date_filed BETWEEN '1895-01-01' AND '1905-12-31'` then check each against bound or body for the sheriff M. J.).
  - **Prior ad-hoc pass** (older tool, pre-2026-05-18): `westlaw-nd-vol1-2-casenames` (8), `vol3-4` (5), `vol5-6` (3), `vol5` (1) — 17 changes; being re-evaluated under the hardened tool per the vol-1 restart.
  - Westlaw names are usually cleaner than NW/NW2d ALL-CAPS headnote-style, but not always right (headnote drift, "et al.", abbreviation style) — hence human-in-loop. Next: resume `--volumes 6-10`.

## 2 · 1953–1996 single-source gap — targeted Westlaw validation

N.D. Reports stopped at vol 79 (1953). **Correction 2026-05-24:** this era is *mostly two-source*, not single-source CL — **archive.ndcourts.gov (`court-archive`) covers 1965–1997** (4,837 opinions; earliest 1965-12-23). Only **183 opinions** in 1953–1996 are *truly* single-source (no `court-archive`/Westlaw/ND second source), and **97 of those are 1955–1959**; the rest scattered. The genuine no-independent-source window is ~1953–1965.

- [x] **Westlaw bulk-download the ~183 single-source 1953–1996 opinions — DONE 2026-05-25.** Pull list `triage/single-source-pull-list-2026-05-25.tsv`. All 183 resolved: **177 ingested** (`westlaw-receive-2026-05-25` + 2 explicit-oid Sletten/Moosbrugger), **2 confidential image-sourced** (Clw 7710, Bh 11482 — `register-nwimage-confidential-2026-05-25`), and **4 were CL −4-cascade duplicates, merged** (Lawson/Scherle/Calavera/Ronngren → their correct-cite 2-source twins; `merge-356nw2d-dups-2026-05-25`). A 5th 356 N.W.2d dup (Bauer 9238→9241) was found by the 1984 audit and merged. Corpus 20,154→20,149. **The 4 "single-source" 356-range entries were never truly single-source** — their twins already had court-archive sources; the §2 gap was inflated by the −4 dups (their cites collided with interleaved Nebraska cases *Vernon* 218 Neb. 539 / *Kaiser* 218 Neb. 556).
  - **Broader audit DONE 2026-05-25:** adjacent N.W.2d volumes **350–362** (169 ND opinions) swept — per-volume all-pairs jaccard, full cross-volume all-pairs (≥0.50), and docket collisions: **0 dup candidates** (the one docket collision, dk 10779 at 353/355, is jaccard 0.11 = two genuinely separate disciplinary orders). The CL −4-cascade was **confined to 356 N.W.2d**; adjacent volumes are duplicate-clean. (A full-corpus dedup sweep remains the parallel session's domain.) Query: `SELECT id,date_filed FROM opinions WHERE date_filed>='1953-01-01' AND date_filed<'1997-01-01' AND id NOT IN (SELECT opinion_id FROM opinion_sources WHERE source_reporter IN ('court-archive','westlaw','ND','archive'))`. Pipeline: `receive_westlaw.py --incoming <dir> --apply` (extracts zips). **Editorial-leak root cause FIXED 2026-05-25**: `receive_westlaw._parse` now runs `_strip_editorial` (West Headnotes excision + Procedural Posture + `strip_synopsis_editorial`) on BOTH parse paths — the leak was mainly the MAIN path (`_parse_westlaw_doc` strips West Headnotes but left the Synopsis stub + "Procedural Posture(s)" line on modern memo-format docs), not just the fallback. Verified on all 183 docs: 0 West Headnotes / 0 Procedural Posture / 0 strippable Synopsis stubs, bodies intact. `strip_westlaw_headnotes`/`strip_westlaw_synopsis` remain the corrective sweep for already-ingested rows. Case-name fix oid 10933 Johnson→Rorvig DONE (`fix-casename-rorvig-10933-2026-05-25`). (Note: the shared `ingest_westlaw._parse_westlaw_doc` still leaves this editorial on the bound-volume path — the ~285-row corpus synopsis backlog; a deeper fix there would benefit all ingest paths.)
- **`date_filed` discrepancy policy (ratified 2026-05-24):** where CL's `date_filed` disagrees with the **court's filing date** (court-archive "Filed" line, or the bound N.D. Reports/`.doc` date), **the court's date governs** — adopt it. Scope scans (read-only, `triage/scan_doc_dates_*.py` + `scan_archive_dates_*.py`): `.doc` era 36/6800 (0.5%); court-archive era 170/4834 (3.5%, mostly ≤14-day filed-vs-decided gaps). Applied via `triage/resolve_date_discrepancies_2026-05-24.py` (guards against adopting later-by->31d dates that may be rehearing/supplemental dates — those are flagged for manual review).
  - **Refinement (2026-05-25, from reviewing the 93 >31d holds):** the policy assumes the court-source date = the opinion filing date, which holds for the ≤31d auto-band but **fails for many >31d holds** — archive pages surface petition/argument/lower-court/rehearing dates and the parser can grab a non-filing one (both *earlier* and, via CL, *later* than the true filing date). **The N.W.2d reporter volume window is the reliable arbiter** (opinions in a volume are filed within ~weeks; whichever candidate date falls in the volume's window is the filing date). Tooling: `triage/classify_date_holds_2026-05-25.py` (window classifier) + `triage/apply_date_holds_verified_2026-05-25.py`. Outcome: 23 adopted (CL had a later rehearing date), 70 kept CL. **28 window-ambiguous holds remain KEEP-CL** — a future per-source pass could check whether CL's date there is also a rehearing date.

- [x] **Build a "low-confidence" batch picker — DONE 2026-05-25** (`triage/lowconf_picker_1953_1996_2026-05-25.py` → `triage/lowconf-1953-1996-2026-05-25.csv`). Ranks all 6,069 opinions in 1953–96 by metadata-anomaly + importance signals (quality_score dropped as planned — empty signal for the era): **NO_AUTHOR (21)**, **JUNK_AUTHOR (9, single-letter OCR truncations)**, **DATE_OUTLIER (0** — dates clean after the 2026-05-25 date-holds + dedup work; >60d outside the N.W.2d volume window), **NAME_ANOMALY (2, trailing "and"/"v."/"Unknown"/too-short)**, + importance boost min(cited_by/15, 3) with **HICITE (63, ≥41 inbound)**. CSV also carries a per-decade RANDOM baseline (8/decade) so Quick Check yield can be compared against a non-flagged control. **31 anomaly-flagged + 63 high-cite** are the priority Quick-Check set.
- [x] **Quick Check the picker's priority set — DONE 2026-05-25.** The 31 anomaly + 63 high-cite priority rows were worked: 10 author recoveries (single-letter→real judge from byline), 2 COA caption-leak fixes (+court field), 18 per-curiam orders, McMahon caption via CL (batches `fix-author-byline-`/`fix-coa-caption-court-`/`fix-percuriam-orders-`/`fix-mcmahon-caption-2026-05-25`). 9770 Papineau confirmed false-positive. See CHANGELOG-data.md.
- [x] **Tag validated opinions so they don't re-queue — DONE 2026-05-25.** Picker now `LEFT JOIN`s `validation_status` and excludes resolved states. **§2-residual triage closed the queue:** the 46 still-`unvalidated` 1953–96 opinions turned out to be **all two-source already** (NW2d/CL + Westlaw, or NW-image) — a tagging gap, not a pull need. Verified each (Westlaw filename caption vs case_name; 4 flagged were coherent caption-style/anonymization diffs) and tagged: **43 `cross_checked`, 3 `single_source_accepted`** (2 confidential NW-image + 12705 Lyons' Estate which has an empty westlaw path) — batch `s2-residual-tag-2026-05-25`. **1953–96 unvalidated/flagged queue now = 0.** The §2 targeted-validation workstream is effectively complete; remaining 1953–96 work is bound-volume on-page order (§10 vol 74) only.
- [x] **35 empty-`source_path` westlaw `opinion_sources` rows — RESOLVED 2026-05-26 (batch `restore-phantom-westlaw-paths-2026-05-26`).** NOT phantom: all 35 are vol-79 N.D. Reports Westlaw `.doc`s (`N.D./79/<page>-<slug>.doc`) that exist on disk; their paths were correctly set by `backfill-westlaw-source-paths` (2026-04-27) then **blanked by a bug in `receive_westlaw._promote`** — `westlaw-receive-2026-05-16` ran with an empty `archive_path` and overwrote both `opinions.source_path` and the westlaw `opinion_sources` row with `''`, then set `is_primary=1` on the now-empty row (line 365-367 matched on the empty path). `text_content` was correctly promoted to the Westlaw bound text; only the file pointer was lost. **Verified each of 35**: N.D. citation page == `.doc` filename page (0 mismatch), file exists (35/35), 2 captions spot-read, 12705 jaccard 0.86 vs NW2d. Restored both `opinions.source_path` (34 westlaw-primary) and the westlaw `opinion_sources.source_path` (all 35) to the absolute `/Users/jerod/refs/nd/opin/N.D./79/...` path (matches the 34 surviving vol-79 siblings). 12705 (NW2d-primary) kept NW2d-primary, westlaw restored as 2nd source, re-tagged `single_source_accepted`→`cross_checked`. **Corpus-wide empty `opinion_sources.source_path` now = 0.** Invariants 22/2/0. Restore script `triage/restore_phantom_westlaw_paths_2026-05-26.py`. **Code-bug guard — DONE 2026-05-26:** `receive_westlaw._promote` now refuses to promote when `archive_path` is empty (returns `NO_ARCHIVE_PATH`, flags the opinion, leaves `source_path` untouched); the caller's dry-run mirrors the empty return so it surfaces pre-apply; new `no_archive` stat counter. Root cause: `_archive_doc` returns `''` for a primary cite with no parseable **N.W.** vol/page (e.g. an N.D.-only cite), which is exactly the vol-79 case. **Residual (open, lower priority):** (a) `_create` is NOT yet guarded — a *new* companion doc with an N.D.-only cite would still be inserted with an empty `source_path`; deferred because skipping it risks dropping a legit opinion (needs a decision). (b) the deeper fix is to teach `_archive_doc` to archive N.D.-only docs to the `N.D./{vol}/` tree (where the vol-79 docs already live) instead of only the N.W.2d tree.

**Gap-era Westlaw receive pipeline (separate from the picker above).** `westlaw_worklist.py` lists gap-era opinions for manual Westlaw Find&Print; `receive_westlaw.py` ingests the returns; `resolve_westlaw_clearwins.py` (now `--report`-aware, defaults to today's batch) drains the surname-anchored clear-wins from the ambiguous queue. Batch 2026-05-16: 506 docs → 488 promoted to authoritative bound text (442 auto + 12 clear-wins + 6 hand-adjudicated shared-cite stragglers + the 46 §6-merged keeps via targeted explicit-oid promote); 18 residual ambiguous are already-resolved. Detailed pool state + next worklist batch live in the `.remember/` handoff, not here.
- [ ] Continue the worklist (pool ~400+ still to list; fix the pool-aging gap so listed-but-unreceived rows get re-listed).

## 3 · 1997–2019 multi-source cross-check

Three sources exist: ndcourts.gov markdown, CourtListener NW2d, archive.ndcourts.gov HTML. All three carry ¶ markers (except CL). Current `opinion_sources` coverage: 1990s 29%, 2000s 90%, 2010s 85%.

- [x] **Close the 1997–2019 second-source gap — SUPERSEDED/REFRAMED 2026-05-18.** Re-scraping archive.ndcourts.gov was rejected: the court PDF is authoritative and the ndcourts.gov markdown is the analyzer's extraction *of that same PDF* (one chain — archive HTML is a redundant re-publication of the same court text, not an independent witness). The real gap is the PDF-era second-source policy, ratified in §9: born-digital embedded-text PDFs are acceptable as single court-derived authoritative; only scanned/OCR-required PDFs (structural detector: ~1 full-page raster/page) mandate a Westlaw copy. Scope 7,424 PDF-era ND → 7,187 born-digital, 41 scanned-without-2nd-source → **all 41/41 Westlaw-sourced** (31 `westlaw-receive-2026-05-18` + 8 explicit-oid + 2 hand-verified `westlaw-pdfocr-residue-2026-05-18`; Lukenbill substituted-opinion resolved `lukenbill-19966-2026-05-18`). Mandatory-OCR worklist closed 2026-05-18.
- [x] Multi-source diff audit: for opinions with 2+ sources, flag pairs where text differs by >N%. **Done 2026-05-04** via `python -m ndcourts_mcp.multisource_diff` (4-word shingle Jaccard, 18 sec on 12,139 multi-source opinions / 16,597 pairs). Report at `triage/multisource-diff-2026-05-04.md`. Distribution: 5,406 near-identical (≥0.95), 4,033 minor-differences (0.85–0.95, mostly OCR), 5,644 moderate (0.70–0.85, mostly westlaw+NW OCR-era noise), 1,177 substantial (0.50–0.70), 225 major (0.20–0.50), and **111 almost-certainly-bug (<0.20)**. Surfaced the major remaining work below.

- [x] **Re-link wrong-paired archive sources** surfaced by the diff audit. **Done 2026-05-04** via `ndcourts_mcp.fix_archive_pairings`. The script indexes every archive HTML's `<title>` (5,345 files, 9,583 unique cites in titles), then walks every `opinion_sources WHERE reporter='archive'` row and compares the file's title-cite against the linked opinion's citations. Found 34 wrong linkages (out of 5,600 archive rows): 4 moves, 3 swaps (with relinks), 27 detaches. Applied as batch `fix-archive-pairings-2026-05-04` (37 changelog rows). Diff-audit <0.20 archive pairs dropped 111 → 93. Report: `triage/fix-archive-pairings-2026-05-04.md`.

- [x] **Investigate the remaining 93 diff-audit <0.20 archive pairs**. **Done 2026-05-04** — characterized in `triage/archive-83-deep-triage-2026-05-04.md`. 10 of the 93 are westlaw+NW (separate item below); the 83 archive pairs split into five distinct failure modes:

  - [ ] **secondary_truncated (30)**: archive HTML body has <30% the word count of the DB primary — HTML download was truncated. Fix: re-scrape from archive.ndcourts.gov via `scrape_archive --reprocess` against the affected opinion list.
  - [ ] **primary_short (26)**: DB primary text is shorter than archive. 2 of these have `Rehearing`/`Modified` markers in the archive (canonical full opinion vs. initial-publication stub in DB); the other 24 are mostly summary orders where DB is intentionally short. Review case-by-case before deciding to update text_content from archive.
  - [ ] **primary_tiny (14)**: DB primary <100 words (summary affirmances). Archive has more only because of HTML chrome inflating the word count. Not a bug — recommend tightening the diff-audit's per-source-pair threshold or skipping pairs where either side is <100 words.
  - [x] **Type Y / contaminated citations (5)**: 3 of 5 fixed 2026-05-04 via `fix_type_y_archive_pairings` (batch `fix-type-y-archive-pairings-2026-05-04`, 6 changelog rows). The Keller pair (13202↔13203) was a one-way move; the two Johnson rows (15805↔15807) were a mutual swap — both opinions had each other's archive linked. Stray neutral cites also removed from the lead rows. The remaining 2 are deeper data bugs:
    - [x] **Kleinsmith / Kitchen (oid 16488 + 15988)**: **Done 2026-05-04** via `fix_kitchen_kleinsmith_2013` (batch `fix-kitchen-kleinsmith-2013-2026-05-04`, 5 changelog rows). Reading the markdown sources confirmed `2013ND18.md` is Kitchen (oid 15988, with wrong case_name) and `2013ND19.md` is Kleinsmith (oid 16488, with wrong archive). Fixes: corrected oid 15988's case_name to "In re Reciprocal Discipline of Kitchen", dropped stray cites from both rows, detached oid 16488's wrong archive, and inserted the correct `archive/2013/20130019.htm` (which existed on disk but was never linked).
    - [x] **Feldmann (oid 16829)**: **Done 2026-05-04** via `fix_feldmann_2017` (batch `fix-feldmann-2017-2026-05-04`, 8 changelog rows). Root cause was a contaminated CourtListener metadata file at `NW2d/889/399.json` that listed `2017 ND 255` as a parallel cite of the Pipeline Petition opinion (which is actually 2017 ND 1, completely unrelated to the Estate of Feldmann case). Fix: dropped the stray cite, repointed source_path from `2017ND255.md` to `2017ND1.md`, refreshed case_name/date_filed/per_curiam, re-read text_content, detached the wrong archive linkage, and re-ran cite_extract for the corrected text (5 outgoing citations). All 5 Type Y candidates from the deep triage are now resolved.
  - [ ] **genuinely_unclear (8)**: both texts substantial and titles match but content differs — likely OCR/formatting drift past the audit threshold or a subtle linkage bug. Individual inspection.

- [ ] **Tighten `fix_archive_pairings` title-check to require neutral-cite agreement.** Surfaced 2026-05-04 by the WSI v. Questar residual (oid 17030): its wrong archive `archive/2017/20170241.htm` is titled `Disciplinary Board v. Lee, 2017 ND 216, 901 N.W.2d 727` — the neutral cite disagrees with the row's `2017 ND 241` but the parallel cite `901 N.W.2d 727` matches (CL has cross-contaminated parallels on multiple opinions), so the current `_title_cite_matches_opinion` returns "ok". Tighten to require the neutral cite match when the title has one; treat parallel-only matches as a weaker signal that needs human verification.

- [x] **Investigate the 10 westlaw+NW pairs at <0.20**. **Done 2026-05-04.** Investigation showed the 10 break into 3 categories:
    - **4 wrong-paired Westlaw .docs** (oids 3011, 699, 5982, 1186) — same shape as the §1 9-pairings cluster. Fixed via `fix_westlaw_pairings_round2` (batch `fix-westlaw-pairings-round2-2026-05-04`); diff audit `<0.20` band 87 → 83.
    - **1 corrupted CL markdown** (oid 4746 *King v. Stark County*): `NW/271/771.md` is HTML/CSS garbage from a failed scrape (ndcourts.gov navigation page leaked into the .md). Upstream data, not fixable from our side. Captured as separate follow-up below.
    - **5 truncated CL OCR markdowns** (oid 1702 Beers, 1701 Schwahn, 1428 Michaels, 1815 Strassheim, 1760 Awes): the CL `NW/<vol>/<page>.md` text is much shorter than the bound Westlaw .doc text — partial OCR or the file is just an extract. Not wrong-paired; the diff audit flags them but they're a data-quality gap rather than a wrong linkage. Capture for the §3 secondary_truncated bucket if we ever do an archive re-scrape.

- [ ] **Detach the corrupted CL markdown** at `NW/271/771.md` (oid 4746 *King v. Stark County*). The file is HTML/CSS junk from a failed CL scrape; the linkage adds noise without value. The Westlaw .doc is correctly paired. Either drop the NW row or leave it as a known-bad data marker.

- [x] **Fix the bug in `align_primary_source`** — already resolved (confirmed stale 2026-05-17). `align_primary_source.py:35-43` is now a `LEFT JOIN opinion_sources s ON … AND s.is_primary = 1` with `WHERE s.id IS NULL OR …`, the docstring/comment explicitly document the no-primary case, and `align()` emits a `[no-primary, UNRESOLVED]` log line plus `no_primary_resolved`/`no_primary_unresolved` stats — exactly the requested fix. No code change needed; item closed.

- [x] **Triage the 919 westlaw+NW pairs in 0.50–0.70** for genuinely-different vs. OCR-noise. **Done 2026-05-13** via 50-pair stratified sample (`triage/westlaw-nw-band-0.50-0.70-sample50-2026-05-13.csv`, script `triage/triage_50.py`). Distribution: **50/50 OCR_NOISE, 0 wrong-pairings, 0 truncations, 0 contamination**. Single consistent failure mode across the sample: Westlaw .doc carries the full bound entry (Syllabus by the Court + statement of facts + opinion), CL NW markdown skips both and starts at the judge's opinion proper, then NW1 OCR drift ("tbe"/"the", star pagination) makes the 4-word shingle Jaccard collapse. No corpus-improvement value from chasing this band. **Action: tighten the diff-audit's per-source-pair threshold for westlaw+NW pre-1953 to <0.40 to drop these 919 noise rows from future reports.**

## 4 · 2020+ source-attachment fix (**the matching rework you flagged**)

The issue isn't missed duplicates — name+date matching actually catches 2020s cross-source pairs (only 2 genuine leaks: Holter 2020-07-22 and Swanson 2021-12-09). The issue is that when a CourtListener NW2d opinion is ingested with parallel `20XX ND X` citations, the dedup merge correctly avoids creating a duplicate row — but the ndcourts.gov markdown file at `~/refs/opin/ND/YYYY/YYYYNDn.md` is never linked as a secondary source. 616 of the 925 ND-primary 2020s opinions carry an NW2d citation, yet only 1 has both source_reporters recorded in `opinion_sources`.

This matters because: (a) we can't text-diff between sources if we never recorded the second source, (b) ndcourts.gov is the authoritative text for 1997+ but NW2d is currently "primary" for 616 of the 2020s opinions, and (c) the same bug almost certainly affects 2000s and 2010s merges as well.

- [x] Write `audit_sources` to measure the gap (2026-04-18).
- [x] Write `backfill_sources` pass: for every opinion with parallel citations, check filesystem for expected markdown/HTML and insert missing `opinion_sources` rows. 5,548 rows linked on 2026-04-18 (batch `backfill-sources-insert`).
- [x] Promote ndcourts.gov to primary for 1997+ opinions where the ND source now exists. 5,260 opinions flipped on 2026-04-18 (batch `backfill-sources-promote`).
- [x] Fix the root cause in `ingest.py` so future ingests record every available source in `opinion_sources`, not just the first one encountered (2026-04-18, commit `4e16481`). Added `_record_source` helper called from all four ingest paths (NW/NW2d fresh + dedup, ND fresh + dedup; ND dedup also promotes to primary for 1997+). Verified idempotent by re-running ingest against the backfilled DB — no duplicate rows, no re-flipped primaries. Spot-checked `merge_nd_metadata.py` (doesn't touch `opinion_sources` — safe) and `ingest_westlaw.py` (already inserts correctly — line 424).
- [x] Align `opinions.source_path` and `opinion_sources.is_primary` with `opinions.source_reporter` (2026-04-18). `align_primary_source --apply` rewrote 5,281 stale source_path values and flipped 34 primary flags on westlaw-sourced opinions where `merge_westlaw_text` had updated source_reporter without touching opinion_sources. Verified integrity: every opinion has exactly one primary source row. Root cause also fixed in `merge_westlaw_text.py` so future runs won't reopen the gap.
- [ ] Text_content alignment: 85 opinions are ND-primary but have no `[¶N]` markers. Inspection showed these are short disciplinary orders where neither the NW2d nor the ND version has paragraph markers, so replacement would be no-op content. No action needed unless a future source (e.g., Westlaw Quick Check) provides a cleaner text for specific ones — capture case-by-case.
- [ ] Resolve the 2 known cross-source leaks that name+date dedup missed: Holter v. City of Mandan (IDs 17670/19516) and Swanson v. Larson (IDs 17913/19616).
- [ ] Resolve Goetz v. Goetz duplicate: opinions 19722 (source: `markdown/2023/2023ND120.md`, case_name "Goetz v. Goetz, et al.") and 20385 (source: `NW2d/988/553.md`, case_name "Goetz v. Goetz") both have citation "2023 ND 53" in the `citations` table — a citation collision from mis-ingest, discovered during the refs-migration audit. One of them likely belongs to a different opinion. Confirm which row is canonical, merge or fix citations.

## 5 · New-opinions pipeline — keep the DB current

`/Users/jerod/code/scraper` has a weekly launchd job (`com.jerod.ndsc-scraper`, Fridays 07:00) that runs `weekly_scrape.sh`. It writes JSON metadata and PDFs to `~/refs/nd/opin/` but is supposed to also write markdown under `markdown/<year>/<year>ND<n>.md` — that step is currently failing silently. The scraper's cache logic treats `existing_data.get('markdown_path')` as "markdown exists" without checking the filesystem.

- [x] Unify source trees so scraper output and ndcourts-mcp ingest agree on paths. `~/refs/opin/` is now backward-compat symlinks into `~/refs/nd/opin/` (2026-04-18, batch `refs-migration-nd-opin`). `ndcourts_mcp.ingest` now reads from `~/refs/nd/opin/`.
- [x] Fix the scraper's markdown-write path in `~/code/scraper/scraper/opinion_processor.py:752` (2026-05-04, uncommitted). The `--skip-analysis` branch now resolves `markdown_path` against `COURT_DATA_DIR` and checks `os.path.exists()`; if the file is missing it falls through to regenerate instead of taking the cached early exit. Mirrors the check the non-`skip_analysis` branch already had at lines 766–781.
- [ ] Once the scraper writes markdown again, verify its output vs. our ingest expectations: paragraph markers `[¶N]`, no weird `I N   T H E   S U P R E M E   C O U R T` spacing. The `markdown_cleanup.py` module in the scraper is supposed to handle this — confirm it's being called in the PDF-extract path. (Spot-checked 2026-05-04: 2026ND72.md and freshly-regenerated 2026ND73.md still carry the `I N   T H E   S U P R E M E   C O U R T` letter-spacing artifact, so `cleanup_markdown` is not handling that pattern.)
- [x] Add a post-scrape step (invoked from `weekly_scrape.sh`) that runs `ingest` + `merge_nd_metadata` + `cite_extract` + `quality_scan` against any new opinions. Log the delta. **Done 2026-05-04** (scraper commit `06c966c`). Stages run independently — `set -uo pipefail` (no `-e`) plus a `record()` helper captures each stage's exit code; final exit is the worst seen so launchd surfaces it. Stats helper prints opinion count + `MAX(date_filed)` before/after.
- [x] Chain the ingest job to the scraper's launchd job. **Done 2026-05-04** — same `weekly_scrape.sh` commit. Single launchd job (`com.jerod.ndsc-scraper`) now runs both stages. The "concerns separate" requirement is met by per-stage exit-code capture rather than a separate launchd job; if stage isolation needs strengthening later (e.g., scrape running 30 min and blocking ingest), split into two jobs at that point.
- [ ] Add a heartbeat check: if `MAX(date_filed)` in opinions.db is more than 3 weeks behind today's date, alert (log line on shell startup or email).
- [x] One-off: verify we aren't missing any 2026 opinions released between the last known release (2026 ND 70, 2026-03-26) and today. **Done 2026-05-04** — 20 missing (ND 71–90 across releases of 2026-04-09 and 2026-04-22), now ingested as ids 20392–20411. See CHANGELOG-data.md "Freshness ingest 2026-05-04".
- [x] **Scraper docket-collision "regression" — NOT REPRODUCIBLE (investigated 2026-05-18).** Live-fetched ndcourts.gov for all canonical cases: 2026 ND 62/73 carry distinct dockets (20240081 / 20240281) live + in current data (the TODO's shared `20250295` was the already-hand-fixed erroneous value); 2024 ND 212 live = correct `11/21/2024`. `web_scraper.parse_opinion_entries` extracts cleanly — one opinion per index row. The `2/14/2024` on 2024 ND 212 in `2024_opinions.json` is isolated **stale historical JSON** (≈ docket-assignment/pending-case date from an early scrape), not a current code defect; the authoritative DB row (oid 19966) is already correct. No parser fix made (would be fixing non-broken code). Test scrape verified: current scraper produces correct, distinct metadata; fetched 2026 ND 100 cleanly.
- [x] **Ingest OOM + the dup-resurrection it was masking — FIXED 2026-05-18.** Test scrape's full `ndcourts_mcp.ingest` was OOM-killed (exit 137): each reporter ran as one monolithic transaction (~12k large `text_content` rows + `opinions_fts` trigger writes accumulating uncommitted in the WAL). Added memory-bounded mode (`--commit-every N`, default 500: periodic commit + `wal_checkpoint(TRUNCATE)` + bounded `cache_size`) — verified full ingest exit 0, peak RSS ~71 MB (was OOM). **Critical discovery the OOM was hiding:** a full re-ingest **resurrects §6-merged-away duplicates** (+307 in the test) — `merge_pair` deletes the dup DB row and re-points its citation to the keep row, but the dropped opinion's source file persists, so a full re-scan re-inserts it. The weekly launchd job runs the full ingest; the OOM crash had been *accidentally* preventing weekly resurrection. **Fix:** added `--incremental` (ND-only, year ≥ DB's latest opinion year; skips the historical NW/NW2d + old-ND trees where merged-dup source files live) — verified idempotent (delta 0, no resurrection), 0.3 s, 37 MB. Wired `tools/weekly_scrape.sh` Stage-2 to `ingest --incremental`. Full mode retained (with `--commit-every`) for deliberate rebuilds. Follow-up (lower priority): a tombstone/`merged_into` guard so even a full rebuild stays idempotent.
- [x] Investigate the misplaced markdown file at `~/refs/nd/opin/markdown/79/673.md`. **Done 2026-05-04** — file was a stale jetcite cache from 2026-04-25 (CourtListener citation-lookup copy of *State ex rel. Minot v. Gronna*, 79 N.D. 673). The opinion is already in the DB (id 12830) with two sources (Westlaw .doc + NW2d markdown), so the stray file was redundant. Deleted with companions in data-repo commit `5653de9`. Ingest now reports 0 errors. Reversible from git history if a third independent source for one 1953 opinion ever turns out to be useful for the §3 multi-source diff audit.
- [ ] Escalation to marker: the scraper's pdfminer extraction fails on scanned/corrected/imaged PDFs. Plumb an escalation path that detects extraction failures (e.g., text < 100 chars/page, or fails the quality score threshold) and re-runs those PDFs through `~/code/markdown-opin/` (marker-based pipeline). Marker is slow (~30s/PDF) so this should be an on-demand escalation, not a default step.

## 6 · Duplicate review queue

**CL double-ingest dedup (2026-05-16).** Built `ndcourts_mcp/merge_opinions.py` — best-of-breed row merge: keep = the inbound-`cited_by` row, drop = the `cited_by=0` CL re-ingest; jaccard≥0.55 guard so distinct shared-page opinions are never merged; conservative `In Re X's Estate → Estate of X` rename only; 13-table constraint-aware re-point; FTS via triggers; full changelog/provenance; `--apply` requires `--limit` (reviewed batches), `--min-jaccard` apply gate, transitive-stale pairs skip + converge on re-run. **230 duplicate rows eliminated** (46 pilot from the 2026-05-16 Westlaw ambiguous queue + 184 corpus-wide pre-1997 jaccard≥0.85). Each pass: snapshot + `align_primary_source --apply` + invariants 0-regressed + 0-orphan-ref verified. Corpus 20486→20256.

**Westlaw-blocked §6 subset progressed (2026-05-17).** The 45 §6-dup-pair-blocked Westlaw-receive oids → 28 pairs → 19 true-dup / 9 distinct-page-mate. **14 clean simple pairs merged** (batch `section6-handadj-2026-05-17`, targeted `merge_pair`; keep = more cited_by, tie→longer text, with a flip-to-westlaw-row refinement so the irreversible drop never discards bound text; corpus 20256→20242; a receive re-run then auto-promoted 7 unblocked survivors to bound text). Detail in CHANGELOG-data.md. **Deferred for per-item human read** (in `triage/section6-deferred-2026-05-17.md`):
- [x] **Borderline trio** — RESOLVED 2026-05-17 (batch `section6-handadj-trio-2026-05-17`). All three read in full → all true dups; merged; all 3 survivors (9059 Anderson, 11201 Dibble, 12028 Dickinson) auto-promoted to Westlaw bound text. The `539 N.W.2d 869` "lean distinct" guess was wrong — jaccard misled by the long consolidated-caption. Corpus 20242→20239.
- [x] **Multi-row clusters** — RESOLVED 2026-05-17 (batch `section6-handadj-clusters-2026-05-17`). All texts read → every internal pair a true-dup. `57 N.W.2d 242` 3→1 (keep westlaw 12544); `521 N.W.2d 643` 4→2 (keep Hosman 11740 + Ferris 11741); `489 N.W.2d 886` 4→2 (keep Woessner 11204 + Putney 11205). 6 deletions, corpus 20239→20233; 12544/11740/11741 promoted to Westlaw bound text, 11204/11205 await future pull.
- [x] **"9 distinct page-mates"** — RESOLVED 2026-05-17 (batches `section6-handadj-pagemates-2026-05-17` + `westlaw-receive-2026-05-17-pagemates`). The "9" was stale: 6 already done by the category-A pass, 2 dissolved by trio/clusters. Of the genuine remainder: `109 N.W.2d 249` + `263 N.W.2d 114` were misclassified true-dups (CL date-drift double-ingest) → merged keeping the correct decision date, then auto-promoted to bound text (corpus 20233→20231); `489 N.W.2d 886` Woessner/Putney explicit-promoted from docs in hand; `356 N.W.2d 897` Calavera + `536 N.W.2d 354` Sletten/Moosbrugger confirmed genuinely distinct, no doc → correctly left queued for own Westlaw pull (not blocked).
- [x] **481 N.W.2d 225** *Disc. Bd. v. Johnson* 11076 — RESOLVED 2026-05-17 (batch `s6-481-johnson-resolve-2026-05-17`). Not a data error: parser gap. 11076 already has the complete clean disbarment order; the Westlaw doc is the same opinion (docket 920029, signatures verbatim) but `_parse` extracts only the editorial Synopsis for the ORDER-format → spurious LOW_SIM. Cross-source confirmed (metadata/disposition/panel verbatim); flag cleared, crosscheck_state→corrected, no text swap. **§6-deferred queue now empty.**
- [x] **Parser gap (corpus-wide)** — RESOLVED 2026-05-17. Root-caused in `_extract_full_bound_text`: the West Headnotes DROP-skip ran past the ORDER body (no Opinion/author header to stop it). Fix: `_ORDER_BODY_RE` + extra stop condition in the skip loop. Verified local: 283/285 incoming docs byte-identical, only the 2 ORDER docs changed; 0/400 corpus-sample docs behavior-affected. `receive` low_sim 2→0; `481 N.W.2d 225` Johnson (11076) upgraded from bookkeeping-only to real bound text, `505 N.W.2d 749` Schmidt (11479) promoted. Future disciplinary-order returns now promote normally.

Remaining §6 work (all in `triage/section6-dedup-corpus-2026-05-16.tsv`):
- [x] **MERGE-HOLD-LOWJAC — FULLY adjudicated 2026-05-18** (fresh dry-run: 146, not the stale 165). Stratified by case-name identity (the true discriminator; jaccard 0.55–0.85 overlaps both classes). **50 pairs merged**: 25 SAFE_TRUEDUP (`section6-lowjac-safedup`) + 10 MID name-format variants (`…-mid-merge`) + 15 MID flagged KEEP_DISTINCT_SUPP that a directed per-pair **text read proved were CL-markdown + Westlaw-bound double-ingests of the same opinion** (author mis-parse + Syllabus-present-vs-absent/OCR jaccard depression — NOT separate authored opinions; `…-supp-merge`, keep=Westlaw-bound). **47 confirmed keep-separate, never merged**: 44 DISTINCT_SHAREDPAGE (different parties — `Hawkins v. Sinclair` ≠ `Burger v. Sinclair`, `In Re Lamb` ≠ `Lamb v. State Tax Comm'r`) + 3 MID REC_DISTINCT (Middlewest Grain family, National Union Fire — different plaintiffs). Corpus 20,229 → **20,179** (−50); invariants 18/2/0 after `align_primary_source --apply`. *Doctrine note: same-name/same-cite/diff-author in the pre-1953 band is usually a CL+Westlaw double-ingest, not a rehearing — text-read, don't trust the heuristic.* Method: explicit per-pair `merge_pair` on adjudicated true-dups only — never a lowered-`--min-jaccard` blind batch-apply (would have destroyed the 47 distinct).
- [x] **NEEDS_DECISION + DEFER_MULTI — FULLY adjudicated 2026-05-18.** NEEDS_DECISION 82 cite-rows → 70 unique opinion-pairs: **16 merged** (`section6-needsdecision-2026-05-18`, text-read CL+Westlaw double-ingests, keep=Westlaw-bound) + **50 confirmed keep-separate** (different-party shared-page companions + 3 supplemental-publication inserts that share the lead's N.D. cite by design — scan false-positives) + 2 stale. DEFER_MULTI 37 cites: **5 merged** (`section6-defermulti-2026-05-18` — Hawkins/Willis-follow-on/Solberg/Pfann/Johnson, text-read same-matter dups), *In re Schirado* confirmed distinct (sequential interim vs final orders), ~14 all-distinct companion clusters, the 5 NEEDS_DECISION-overlapping clusters auto-cleared, 3 already done 2026-05-17. Corpus 20,180→20,159 (−21). Method: explicit per-pair `merge_pair` on text-read true-dups only; snapshots `opinions.db.bak-pre-s6-needsdecision-2026-05-18` + `opinions.db.bak-pre-s6-defermulti-2026-05-18`; invariants 18/2/0; 0 orphans. Worklists committed (`triage/section6-{needsdecision,defermulti}-worklist-2026-05-18.tsv`).
- [ ] **~530 DEFER_MODERN** — post-1997 (date≥1997) released-corpus pairs, deferred for separate vetting (touches the public release + the 258 neutral_cite_uniqueness baseline; some CONFIDENTIAL juvenile matters; treat carefully). Now also includes the 9 post-1997 DEFER_MULTI clusters reclassified here on 2026-05-18 (612 N.W.2d 278, 638 N.W.2d 45, 709/711/719/742/756 N.W.2d, 766/777 N.W.2d).
- [ ] **Cite-hygiene follow-ups** (surfaced by the 2026-05-18 NEEDS_DECISION pass; not row merges): (a) `55 U.S.L.W. 2667` carried as an opinion citation on two distinct 1987 opinions (FLB v. Lillehaugen 9867, Hanson v. Hanson 9868) — a foreign secondary cite that should be stripped per the 2026-05-17 foreign-cross-ref policy; likely more USLW-tagged rows corpus-wide. (b) A handful of distinct shared-page pairs where one side carries a contaminated N.D. parallel (CL cross-contamination, e.g. *White v. Lauder* `10 N.D. 400`, *Edwards v. Eagles* `107 N.W. 43`) — low priority, not row-count-affecting.
- [ ] **120 post-1997 opinions carry no neutral cite** (period-tolerant check; 7427/7549 do). Mostly summary/disposition-table entries sharing one N.W.2d page (2018–19 spike); confirm none are real opinions missing a `YYYY ND n` cite (matters for dedup completeness — copies that share no cite never collide). Owed: a triage list for review.
- [ ] Legacy: 667 unreviewed citation-overlap `duplicate_candidates` rows via the Dup Queue UI (overlaps the above; the merge_opinions scan is the more direct lever now).
- [ ] Keep the name+date+text strategy running on new ingests; stays at 0 unreviewed currently.

## 7 · Missing source files — small gaps

Cases where the opinion record claims a source but the underlying file is absent. Discovered by `audit_sources`.

- [ ] Recover `~/refs/nd/opin/markdown/1997/1997ND5.md` (State v. Torres, 1997-01-16, opinion id 20383). Not on disk; adjacent files 1997ND50–59 exist. Re-scrape from ndcourts.gov or restore from `~/refs/nd/opin/1997_opinions.json`.
- [ ] Recover `~/refs/nd/opin/markdown/1998/1998ND22.md` (State v. Schmidt, 1998-01-22, opinion id 20384). Same pattern — adjacent 1998ND220–228 exist but not the single-digit file.
- [ ] Investigate whether the missing-file pattern is systemic to low-numbered ND opinions (ND5, ND22 ⇒ possibly naming collision with ND50/ND220 during download). Re-run `audit_sources` periodically to catch new gaps.

## 8 · Quality and metadata cleanup — long tail

- [x] **18 opinions with `overall_score < 0.5` — RESOLVED 2026-05-26.** They were NOT OCR-garbled: the scorer counted U+00A0 (non-breaking space, pervasive in Westlaw `.doc` exports) and `¢` as "garbage" and applied an *absolute* per-char penalty (`score -= garbage_chars`), so a long clean opinion with ~70–300 NBSPs went negative → clamped to 0.0. 15 of 18 were also **stale** (scored 2026-04-06, then Westlaw-promoted later). Fix: added U+00A0 + `¢` to `_LEGIT_NONASCII` in `quality_scan.py`, corpus-wide `--rescan` (range now 8.5–84.5, avg 59.2). **`<0.5` queue = 0**; the original 18 now score 37.5–68.0. One genuine corruption found & fixed: **13240** *Johnson v. Johnson* (2000 ND 170) had its analyzer `[¶N]`/`§`/`—` markers mojibake'd through a Thai (CP874) codec (`ถ`×212→¶, `ง`×75→§, `โ\x80\x94`×9→—); demangled in place (`demangle-13240-2026-05-26`, counts verified against the pristine on-disk file). **Newly surfaced (separate, lower priority):** the new low end (<25) is 5 opinions — 4 HTML-contaminated (`has_html=1`: 9554, 10112, 10982, 3434 — overlaps §8/§3 HTML cleanup) + 1787 *Bayne v. Thorson* (38 real garbage chars). **NEW TODO:** 13240's `text_content` is ~237 KB = the ND markdown (¶-marked, repaired) **concatenated with** the NW2d OCR copy (~119 KB) — a double-source ingest defect; de-dup needs a re-ingest from the clean ND markdown, not a blind truncation.
- [x] **79 opinions with no author and not per_curiam — RESOLVED 2026-05-26** (was 90; `fix-noauthor-2026-05-26`). Per-item verified split: **5 author recoveries** (byline missed by the empty author field — 441 Morgan C.J. post-syllabus, 20388 Christianson & 20390 Robinson separately-published concurrences, 20391 Gronna rehearing, 15216 Sandstrom = 2009 ND 113 confirmed via CL); **34 per curiam** (`per_curiam=1` — genuine `PER CURIAM.` dispositions incl. pre-1953 + modern Rule 35.1/discipline per curiams, + 12570 Fisher = 5-justice signed ORDER OF SUSPENSION); **40 left untouched** = authorless summary dispositions ("Affirmed by summary opinion"/"Affirmed.") that carry no byline and no per-curiam label (CL records them `author:null, per_curiam:false` — confirmed against CourtListener). `auto_author` only scans non-null authors, so the empty-author set needed this manual byline/per-curiam triage. Residual queue now = 40 (all the summary-disposition stubs, correctly authorless).
- [ ] Rebuild pre-1997 judges field (panel membership) from `justices.py` service dates plus disqualification lines in opinion text. Currently unusable from CourtListener metadata.
- [x] **Contract DEFINED & ratified 2026-05-17** → `SCHEMA.md` (reporter taxonomy: `ND-neutral`/`ND`/`NW`/`NW2d`/`NW3d`/`ALR`/`LRA`/`US`/`SCT`/`LED`; `is_primary` = single official cite, ladder neutral>ND>NW3d>NW2d>NW, secondary never, exactly one/opinion).
- [x] **A.L.R./L.R.A. mis-classification + the `is_primary` contract — APPLIED 2026-05-17.** Executed the full SCHEMA.md plan: classifier+`recompute_primary` rewrite, ~12 consumers updated in lockstep (source_reporter axis untouched, verified by 0-change `align_primary_source` dry-run), batch `reporter-taxonomy-2026-05-17` (14,568 reclassified incl. A.L.R.→`ALR`/L.R.A.→`LRA`, 461 foreign cross-refs deleted per user ratification, 79 NULL resolved → 0 NULL / 0 out-of-taxonomy), batch `is-primary-recompute-2026-05-17` (24,183 flips fixing the 817 multi-primary + systemic regional-over-official → 0 multi / 0 zero / 0 secondary primary), 3 new invariants green at 0. Dashboard 18 ok / 2 known / 0 regressed. Tools `migrate_reporter_taxonomy.py` + `recompute_is_primary.py` retained for revert/audit.
- [x] **7 flagged cite-collisions — RESOLVED 2026-05-17** (per-item Word review): 4 distinct-shared-N.W.-page cite fixes (`fix-cite-discrepancy-flagged-2026-05-17`: #1/#2/#3/#5), 1 pairing scramble (`fix-middlewest-pairings-2026-05-17`: #4 Olson/Nelson@250), 2 §6 dups (`section6-flagged-collisions-2026-05-17`: #6 Ligaarden, #7 Ellis v. Fiske). All 37 Type-Y-sweep cite bugs now closed. See CHANGELOG-data.md.
- [x] **`298 N.W.2d 372` shared by two distinct cases — RESOLVED 2026-05-25 (bound vol 298 image).** oid 8266 *Boedecker v. St. Alexius Hospital* (Civ. No. 9767, 1980-10-30) and oid 20482 *In re Disciplinary Action Against McMahon* (No. 9723, 1980-08-20, per curiam arson disbarment) both legitimately cite `298 N.W.2d 372`: the bound page (Westlaw image `~/Downloads/298_N.W.2d_372.pdf`) shows the short McMahon disbarment order at the **top** of p.372 and *Boedecker* **beginning at the bottom of the same p.372** (running through 375). A short order ending and the next case starting on one page is a legitimate **shared starting page**, not a mis-pagination — both cites are correct. McMahon docket recovered (`No. 9723`, batch `fix-mcmahon-docket-2026-05-25`); 20482 → `cross_checked`. **Residual (low priority):** a bare citation to "298 N.W.2d 372" is genuinely ambiguous between the two; the 8 `cited_by` attributed to McMahon (20482) most likely belong to *Boedecker* (the substantive med-mal/jurisdiction opinion; the disbarment order is rarely cited) — re-check `cite_extract` attribution if it matters.
- [x] **`merge_pair` dup-row defect — FIXED 2026-05-17.** Root cause (`_dedup_simple` blind `UPDATE` left duplicate `(opinion_id,citation)`/`(opinion_id,source_path)` rows on every §6 survivor, pre-existing corpus-wide). Code fixed (`_repoint_unique`), 256 opinions / 292 citation + 16 source dup-rows cleaned (`fix-dup-citation-rows-2026-05-17`, changelog-revertible), 2 new invariants added (`citation_row_unique_per_opinion`, `source_row_unique_per_opinion`, both keyed per-opinion so cross-opinion shared cites are preserved). Dashboard 15 ok / 0 regressed.

## 11 · SCHEMA.md data dictionary (planned)

No per-field documentation exists today (only the `changelog` table has inline comments; `db.py` schema is partially stale vs the live DB). Nearly every non-trivial snag this session traced to an undocumented field contract (`is_primary`, `reporter` valid values, `source_path` absolute-vs-refs-relative resolution, `crosscheck_state`/`era_tier` enums, cross-opinion citation-uniqueness semantics). Write `SCHEMA.md`: per table/column — purpose, type, valid range/enum, nullability, FK relationships, and the invariants/contracts that hold (incl. the shared-page citation model, the `source_path` resolution rule, the `is_primary` contract once defined, the §10 synthetic-neutral-cite identifier). Reconcile `db.py` `create_schema` to the live schema so there is one source of truth. Sequence: do it *after* the in-flight data-correctness items (A.L.R./`is_primary` contract) so it documents settled, not buggy, contracts.

## 9 · Definition of "fully validated"

An opinion is considered validated when:
- It has at least two independent source texts linked in `opinion_sources` (OR it sits in the 1953–1996 single-source era and has been either auto-verified against Westlaw Quick Check or manually spot-checked), AND
- Its author, date_filed, and case_name have been confirmed against a non-OCR source (ndcourts.gov, Westlaw, or bound reporter), AND
- `overall_score >= 0.5`, AND
- Parallel citations are populated from metadata, not just the ingest source.

**PDF era (1997+ ndcourts.gov) — ratified policy 2026-05-18.** The court PDF is the authoritative copy; the linked markdown is the analyzer-extracted text of that same PDF (one chain, NOT an independent second source — see SCHEMA/memory provenance). Policy:
- **Use a second source whenever available**, including proactively downloading a Westlaw copy to obtain one.
- **A Westlaw copy is MANDATORY for any PDF-era opinion whose text required OCR to extract** (scanned/imaged PDF, no embedded text layer). For cleanly-extracted embedded-text PDFs a single court-PDF-derived source is acceptable as authoritative.
- "Required OCR" signal (resolved 2026-05-18): authoritative detector is **court-PDF structure** — a scanned/OCR-required PDF carries ~one full-page raster image per page; born-digital carries ~none (`triage/classify_pdf_extraction.py`). The `quality_scores.ocr_artifacts` proxy and `case_cache.db` were rejected (proxy under-detects silent-clean OCR; cache empty). Scope: 7,424 PDF-era ND opinions → 7,187 born-digital (single court source OK), 68 scanned, **41 scanned-without-2nd-source** → mandatory-Westlaw worklist (`triage/westlaw-pdf-ocr-worklist-2026-05-18.md`).
- Status (closed 2026-05-18): **all 41/41 Westlaw-sourced.** 31 via `westlaw-receive-2026-05-18`; the 10 residue via `westlaw-pdfocr-residue-2026-05-18` (8 explicit-oid promotions resolved by unique neutral cite; 2 hand-verified manual promotions — *Neset* 1998 ND 206 ORDER-format, *Richardson* 2020 ND 246 per-curiam memo — `_parse_westlaw_doc` extracted no body for these). Lukenbill (2024 ND 212) hand-resolved as a substituted-opinion case (`lukenbill-19966-2026-05-18`). All 41 now carry ≥2 sources.

Progress metric to track: `% of opinions meeting all four criteria`, by decade, in `get_database_stats`.

## 10 · Planned enhancement — synthetic medium-neutral cite as universal unique ID

**Goal.** Extend the post-1997 medium-neutral citation format (`YYYY ND N` — e.g. `1997 ND 201`, `2020 ND 30`) **backward to every opinion**, including pre-1997, and use it as the corpus's stable unique identifier.

**Rationale.** Pre-1997 opinions were not published with the `YYYY ND N` neutral format, and the reporter-page cite is *not* a unique key in that era — the old per-curiam clusters print multiple distinct short opinions on one shared N.D./N.W. page (e.g. *Sykes v. Allen* and *Montgomery v. Tucker* both at `12 N.D. 504, 96 N.W. 1134`; see the #1–#5 flagged-collision work). A back-assigned `YYYY ND N` gives each opinion — including each member of a shared-page cluster — a single unambiguous identifier, and lets `neutral_cite_uniqueness` extend corpus-wide instead of only 1997+.

**Design sketch (not final):**
- Assign `YYYY ND N` where `YYYY` = filing year and `N` = sequence within that year, ordered by `date_filed`, then N.D. page, then **on-page order for shared-page clusters**.
- **Shared-page disambiguation:** each distinct opinion on a multi-opinion bound page gets its own sequence number — that is the mechanism that finally makes shared-page opinions individually addressable (resolves the lookup-ambiguity limitation noted in the shared-page model discussion).
- **Provenance is mandatory.** Back-assigned cites are *synthetic/editorial*, not how the case was published. They MUST be stored and surfaced as distinctly synthetic (separate reporter type / flag), never presented as an original citation. Misrepresenting a 1903 opinion as natively `1903 ND 47` would violate the accuracy/fair-notice bar that drives the authoritative-text goal — a court adopting the corpus must be able to tell native neutral cites (1997+) from back-assigned ones.
- **Human review required:** bound-volume pages carrying multiple opinions must be human-reviewed to fix the correct on-page publication order before sequence numbers are assigned (order must reflect true publication order, not ingest order).

**Open design questions:** storage (dedicated `neutral_cite` column on `opinions` vs a flagged `citations` row of a new reporter type); interaction with the existing 258-pair `neutral_cite_uniqueness` baseline once scope extends pre-1997; renumbering policy if an opinion is later inserted/removed (stability vs density); how synthetic cites appear in `lookup_opinion`/`cited_by` resolution. To be specified before implementation; capture the settled contract in the future `SCHEMA.md`.

**Design RATIFIED 2026-05-19.** Settled in CHANGELOG-data.md `nw-bound-image-source-section10-spotcheck-2026-05-19`:
- Storage: `citations` row with new taxonomy value `ND-neutral-synthetic` (extends Contract 1 enum); `is_primary=0` always.
- Format: `YYYY ND nnn`; ordering key `(date_filed, ND-starting-page, NW-starting-page, on-page-order)`.
- Visual marker: bracketed `[YYYY ND nnn]` in MCP / `lookup_opinion` output to distinguish from native cites.
- Scope: all pre-1997 (`date_filed < '1997-01-01'`, ~14,000 opinions); the 258 post-1997 neutral_cite_uniqueness baseline is §6 not §10.
- New invariants: `synthetic_format`, `synthetic_uniqueness_per_year`, `synthetic_only_pre_1997`, `synthetic_never_primary`.

**Spot-check verdict 2026-05-19**: the `(N.D.-page, N.W.-page, date)` ordering heuristic confirmed on 4 of 5 bound-page samples (10 N.D. 400, 14 N.D. 557, 100 N.W. 1084 3-cluster, 111 N.W. 614 4-cluster — all match printed order). The 5th sample (`96 N.W. 1134` Sykes/Montgomery same-NW/diff-date) failed delivery (user got Minn case from disambiguation). Residual untested category = "same-NW / diff-date" (3 clusters in 1B set: 12 N.D. 504, 68 N.D. 310, 78 N.D. 10) — low risk, can spot-check later if anomalies surface during assignment.

**Phased implementation plan** (deferred to a later session):
- Phase 1A: chronologic assignment for the ~6,200+ opinions on a unique N.D. page (no ambiguity).
- Phase 1B: 32 digitally orderable shared clusters using `(nw_page, date)` (theory confirmed).
- Phase 1C: 5 Emmons clusters using bound pages already on disk (NW/84/1117.pdf + 1118.pdf).
- Phase 1D: 3 supplemental/follow-on pairs using by-design ordering (lead first).
- Phase 2: ~20 truly-tied clusters needing a second Westlaw bound-page pull (cites listed at need; see `triage/casenames-mispaired-...` workflow).

**Refinements & decisions 2026-05-24:**
- **Stability policy (user-ratified):** synthetic numbers are PROVISIONAL until a publication freeze — renumber freely as opinions are added/de-duped, always keeping correct `(date, page, on-page-order)` order (do NOT append mid-year additions to the end of the sequence). Lock the numbers at publish; after that, treat as a stable citation. Goal before locking: best effort to have ALL opinions and ZERO duplicates. **Interim unique identifier = `opinions.id` (oid)** — INTEGER PRIMARY KEY, 20,170 distinct, no nulls, never reused (merges delete the dropped row). Use oid for cross-referencing until synthetic cites lock.
- **Reporter-order guard (user-ratified):** the N.D. Reports and N.W. Reporter may print same-page opinions in DIFFERENT order. For a synthetic *ND* cite, pull BOTH the N.D. and N.W. page images for each genuine shared-page cluster rather than trusting N.W.-page order as a proxy; where the printed date is the same and the two reporters disagree on order, MANUALLY adjudicate (N.D. Reports order governs the ND cite).
- **Corpus structure (measured 2026-05-24):** 12,620 pre-1997 opinions; **12,485 (98.9%) are Phase-1A trivial** (unique `(date, page)` within year). Only **66 collision clusters (135 opinions)** need on-page ordering: 52 bound N.D. + 14 NW2d-era. Of the 52 bound: 22 order by distinct N.W. page (but verify vs N.D. order per the guard), **30 truly collide** (same date + ND page + NW page).
- **DEDUP PREREQUISITE — DONE 2026-05-24.** The `X v. Y` + `Re X`/`In re X` twins (same docket + same N.D./N.W. page) were text-read: all confirmed one opinion double-ingested (the `Re X` caption names the same parties + docket). **18 merged** (`section6-rex-twins-2026-05-24`, keep the Westlaw-bound `X v. Y` row) + the **61 N.D. 179** pair (`fix-61nd179-dup-2026-05-24`). Corpus 20,172→20,154. **Lesson (saved to memory):** same-page + high-jaccard ≠ dup — *read whether the parties/docket match*. The Emmons memoranda (different defendants, identical boilerplate, distinct dockets) were KEPT separate (3 even ingested); the Re-X twins (same parties, two captions, same docket) were MERGED. The genuinely-distinct remainder (Kellogg/Globe, Tallmadge/Mercer, McAndress/Koch, McCue, Emmons block) keep their own rows and need on-page order only.
- **Bound-volume scans + sourcing convention (2026-05-24):** full N.D. Reports volume scans (Google Books image PDFs) are filed at **`~/refs/nd/opin/N.D./<vol>/_bound-volume.pdf`** (co-located with that volume's per-opinion `.doc`s). Page→PDF offset is per-volume (vol 3 +44, vol 9 +110, vol 44 +17); render with `mutool draw -o p.png -r 160 <vol>.pdf <pdfpage>` and read the running-header page number to calibrate. Opinions sourced from a bound volume use `opinion_sources.source_reporter='ND-image'` + `source_path`=the volume scan (parallels `NW-image`). **Westlaw images are the N.W. Reporter, not the N.D. Reports**, so the N.D.-vs-N.W. order check needs these bound scans. **On disk now: vols 1, 3, 9, 19, 20, 34, 44** (`N.D./<vol>/_bound-volume.pdf`). **Still to pull: vol 74** (last Tier-1 — has a distinct-tied cluster + dup-candidates).
- **Order results (2026-05-24):** 3 N.D. 538 Globe→Kellogg (N.D.=N.W. confirmed from vol 3); 44 N.D. 247 Nelson(H.J.)→Lammadee (NO gap — full Middlewest block present 246–250, N.W. reporter just packs them tighter); 9 N.D. 608–615 Emmons full N.D. order recorded (CHANGELOG `ingest-emmons-additions-2026-05-24`), 3 missing memoranda ingested (oids 20500–20502, marked DO-NOT-MERGE), 2× Cranmer / 2× Davidson confirmed distinct. N.W. on-page order also read for 19/20/34 (McCue Soo→Great Northern; McAndress→Koch; Tallmadge→Mercer) — verify against the now-on-disk N.D. vols 19/20/34 per the guard.

**WHERE A FRESH AGENT PICKS UP (§10, after 2026-05-24):**
1. **Pre-1997 dedup for the shared-page bound era is DONE** (Re-X twins + 61nd179 merged; Emmons reconciled). Corpus **20,154**.
2. **Phase 1A is DONE 2026-05-24** (`section10-phase1a-synthetic-2026-05-24`, see CHANGELOG-data.md). All **12,604** pre-1997 opinions now carry a synthetic `YYYY ND nnn` cite (`reporter='ND-neutral-synthetic'`, `is_primary=0`), ordered by `(date, ND-page, NW-page, oid)`. Taxonomy + 4 invariants live; dashboard **22 ok / 2 known / 0 regressed**; `neutral_cite_uniqueness` held 258. Tool: `triage/section10_phase1a_2026-05-24.py`. Revert deletes the batch cleanly.
3. **Remaining ordering work — within-cluster order is PROVISIONAL** (tie-broken by oid). The 101 opinions in 48 genuine shared-page clusters need true on-page order (worklist `triage/section10-cluster-worklist-2026-05-24.tsv`):
   - **Known orders applied 2026-05-24** (`fix-section10-cluster-order-2026-05-24`): 3 N.D. 538 (Globe→Kellogg, was reversed) and the full 9 N.D. 608–614 Emmons block (read from the bound vol 9 scan this session). 44 N.D. 247 was already correct. Tool: `triage/fix_section10_cluster_order_2026-05-24.py`.
   - **All on-disk-volume clusters verified 2026-05-24** (`fix-section10-cluster-order-ndverify-2026-05-24`): read every cluster in vols 9/19/20/34/44 against the bound scan. Correct as-is: 19 N.D. 57, 20 N.D. 370/372/412, 34 N.D. 601. Reordered: 9 N.D. 615 (Wilson→Douglas) and 44 N.D. 250 (Nelson→Olson, N.D. governs over the N.W. tie). Offsets: vol 9 +110, 19 +18, 20 +22, 34 +26, 44 +17.
   - **Both data-quality flags RESOLVED 2026-05-24** (`fix-bound-discrepancies-2026-05-24`): (a) 9 N.D. 615 — *Wilson* (5747)→1900-11-13, *Douglas* (5774)→1900-11-24 (DB had both 12-08); (b) 44 N.D. 250 — *Nelson* (2169) N.W. cite→`173 N.W. 475`. The date change moved Wilson into the Nov-13 Emmons block, so the 1900 sequence was resequenced (`section10-resequence-2026-05-24`, 23 cites).
   - **CANONICAL ordering tool now = `triage/section10_resequence_2026-05-24.py`** — recomputes the whole pre-1997 sequence from current DB state + verified `KNOWN_ORDER` on-page orders and applies only changed cites. **Re-run it after any pre-1997 data correction** (date/de-dup/ingest). Supersedes the one-shot `fix_section10_cluster_order` / `fix_section10_ndverify` scripts; add newly-verified cluster orders to its `KNOWN_ORDER`.
   - **On-disk bound N.D. volumes: 1, 2, 3, 7, 9, 10, 13, 14, 16, 17, 18, 19, 20, 24, 27, 34, 41, 44, 47.** vols 14 & 24 verified (`-24b`); vols 10/27/41/47 verified 2026-05-24 (`fix-date-bound-volume` + `-24e`): 27 N.D. 458, 41 N.D. 473 (Druey lead+separate), 47 N.D. 266 (Altenbrun lead+concurrence) all order-correct; 47 N.D. 266 got a 4-day date fix (→1921-01-28). **vols 16/17/18 verified 2026-05-25 — NO changes** (5 clusters; see CHANGELOG): 16 N.D. 59 (Kerr trio), 16 N.D. 106, 17 N.D. 393 all order-correct; 17 N.D. 266 (Skeffington/Ross) & 18 N.D. 76 (Willis) are lead+same-day-dependent-per-curiam pairs printed companion-on-top → kept **lead-first** per the 2026-05-25 ruling. Willis lead kept at 18 N.D. 76 (caption-page convention; per curiam's "77" is an opinion-text pinpoint). **10 N.D. 400 *White v. Lauder* knot RESOLVED 2026-05-25** (two distinct companion opinions; 5834 date+NW-image fixed — `fix-white-lauder-2026-05-25`, see CHANGELOG).
   - **vol 21 verified 2026-05-25 — NO changes** (offset +22; 21 N.D. 69 Willis-v-Weatherwax/Kramer-v-Kiefer and 21 N.D. 348 Lanpher-Skinner/Heard-v-Holbrook both match the NW-page tiebreak = bound top-to-bottom order). **vols 23 & 32 need NO verification** — each has a single shared page that is *different-date* (23 N.D. 352, 32 N.D. 373), so the date sort already orders it.
   - **Remaining bound on-page work — vol 74 only** (NOT on disk and NOT on archive.org as of 2026-05-25). **HeinOnline serves individual case scans** for the gap-era volumes archive.org lacks (50/64/74) — preferred source.
     - **vol 64 — 64 N.D. 367 — DONE 2026-05-25** (HeinOnline scan, filed `N.D./64/367-hein-scan.pdf`): bound prints *Ott v. Kelley* (4484, File 6233, per curiam) above *Thorvaldson-Johnson* (4482, File 6234) — N.D. order by file no., inverse of the N.W. tie. `KNOWN_ORDER [4484, 4482]`; resequenced (`section10-resequence-2026-05-25`): Ott→1934 ND 5, Thorvaldson→6. Invariants 22/2/0.
     - **vol 74 — 74 N.D. 242** (need pp. **241–243**): oids 7179 (*N.P. Ry. v. McDonald*, per curiam following *application of Hanson*) & 7180 (*In re Adams*, Burr J. full opinion) share **docket File 6966 + 21 N.W.2d 351 + parties** but distinct CL cluster_ids → likely a double-ingest or lead+companion split. Resolve as one-opinion-vs-two (§6-style merge) **before** ordering; the bound page tells which.
     - **vol 50 — 50 N.D. 25 — RESOLVED without the volume** (2026-05-25): *Lincoln Addition v. Lenhart* lead (2863, 195 N.W. 14, full opinion) + its same-day per curiam Mem (20390, 195 N.W. 33, "joint with Baker v. Lenhart"). Lead+dependent → **lead-first** per the ruling; default NW order already complies (14<33). Provisional 1922 ND 171/172 correct. No KNOWN_ORDER entry.
   - Resolve the **28 NW2d-era** member rows (1953–96, no N.D. volume — need NW2d page pulls).
   - **§10 sub-item — `White v. Lauder` knot — RESOLVED 2026-05-25** (`fix-white-lauder-2026-05-25`). Read bound 10 N.D. 400 + 445 + N.W. 996/1135: **two distinct companion mandamus per curiams** (both follow Gunn, both 87 N.W. 1135, both filed 1901-11-21) — 5834 at 10 N.D. 400 (with syllabus), 5833 at 10 N.D. 445. Kept both (ruled). Fixed 5834 date 1901-10-25→1901-11-21 (10-25 was *Willard v. Monarch*'s date on the same bound page) and its NW-image 996→1135 (996 is Willard's N.W. page). See CHANGELOG.
4. **Display follow-up:** surface synthetic cites bracketed `[YYYY ND nnn]` in `lookup_opinion`/MCP output to distinguish from native cites (ratified visual marker; not yet wired into the server).
5. **Cite-hygiene follow-up noted:** Benness oid 2172 date 1919-06-21 (DB) vs "June 27, 1919" (bound vol 44) — low priority.

## 12 · Functionality enhancements — MCP tools for jetredline / jetmemo / human research

New server tools to build once the corpus is validated. Grouped by the workflow each
serves. Feasibility is rated against current data: full `text_content` with `[¶N]`
markers (modern), "Syllabus by the Court" (pre-1953), the `citations` parallel-cite
rows + reporter/`is_primary` taxonomy, `get_citing_opinions` relationships, and
judge/date/docket metadata. (Brainstormed 2026-05-24; one idea — syllabus-only search —
deliberately omitted per user.)

**Highest leverage for jetredline (cite + quote proofreading): ALL BUILT 2026-05-24** — added to `server.py` as MCP tools with pure logic in `ndcourts_mcp/proofread.py`; verified against the live DB (modern ¶-marked + pre-1997 OCR, drift, fuzzy/typographic quote variants, ambiguous-name disambiguation). Synthetic `[YYYY ND nnn]` cites are surfaced bracketed and never folded into a formatted citation (preserves the §10 native-vs-synthetic distinction).
- [x] **`verify_citation`** — given a cite string or case name, confirm it exists and return the *canonical* form: exact case name, date, authoring justice, and the full parallel-cite set in Redbook order. Catches wrong volume/page, wrong year, name drift. Single biggest redline win. *High feasibility — lookup + formatting.* **DONE:** `verify_citation(query, expected_case_name=None)` — when both a cite and the as-written name are given it returns `name_matches` + `name_similarity` to flag drift; returns `cites_redbook` (ordered structured rows) + a ready-to-paste `formatted` string; ambiguous names return a `candidates` list.
- [x] **`verify_quotation`** — given a quoted passage attributed to a case, locate it in `text_content` and confirm it is verbatim, returning the pinpoint `¶`. Flags misquotes, dropped words, bad pin cites. *Very feasible — full text available; modern opinions give ¶-level pinpoints free.* **DONE:** formatting-tolerant (whitespace, curly/straight quotes, dash style treated as still-verbatim); word changes/omissions flagged with a word-level diff + `closest_text`; case-only mismatch flagged separately; pinpoint `¶` returned where markers exist (pre-1997 returns a `paragraph_note`).
- [x] **`get_parallel_citations` / cite auto-complete** — feed any one cite, get the complete parallel set so a draft's missing neutral or N.W.2d parallel auto-fills. *Trivial over the citations table.* (Overlaps §10's parallel-cite work.) **DONE.**
- [x] **`get_pinpoint`** — given case + `¶N` return that paragraph; or given a quote return the `¶` it lives in. Supports pin-cite insertion/verification. **DONE:** exactly one of `paragraph`/`quote`; returns the paragraph text + a `pinpoint` cite string (`<primary>, ¶ N`).

**Highest leverage for jetmemo (authority research): ALL BUILT 2026-05-24** — added to `server.py` (tools) + `ndcourts_mcp/memo.py` (pure helpers: disposition/syllabus extraction, citing-context, treatment classifier); verified against the live DB incl. a real overruling (`State v. Herrick`, 1997 ND 155).
- [x] **Citator / "still good law" check** — aggregate `get_citing_opinions` and scan citing sentences for treatment language (overruled, abrogated, superseded, distinguished, followed, criticized). A lightweight version returning the citing sentence + surrounding `¶` is a real KeyCite/Shepard's substitute. *Highest value, hardest — retrieval half is easy; treatment classification needs NLP on the citing sentence.* **CAUTION: a citator that misclassifies negative treatment is worse than none — a "still good law" false-negative on an abrogated case is a real harm. Keep the treatment signal conservative and ALWAYS return the underlying citing text for human verification; never surface a bare green/red flag.** **DONE as `check_treatment` (user-ratified "conservative signal + always text"):** per-citation signal is sentence-local and labeled `possible negative — verify` / `distinguished — verify` / `possible positive` / `neutral`; NO overall good-law verdict; the citing context is ALWAYS returned. Context window is paragraph-bounded and deliberately NOT trimmed on ". " (legal abbreviations like `v.`/`N.W.` create false sentence breaks that would drop a treatment verb sitting *before* the case name — a false-negative risk). Possible-negative/distinguished items are sorted first so the `limit` never truncates them away; `signal_counts` are reported over the scanned set.
- [x] **`get_cited_authorities`** — inverse of citing: the outbound cites *this* opinion relies on, to map the authority graph around a case on appeal. **DONE:** groups `text_citations` by `cite_type` (cases / statutes / court_rules / constitution / regulations) with source URLs; in-corpus ND cases resolved to case_name/date/oid via the `citations` join.
- [x] **`case_summary` bundle** — one call returns bench-memo front matter: name, all cites, date, author, panel, syllabus points, disposition, `¶` count. Saves jetmemo many round-trips. **DONE (user-ratified "include derived, clearly labeled"):** structured fields + Redbook-ordered cites (synthetic separate) + panel/voting (1997+) + cited-by/cites-out counts; `disposition` (heuristic, modern all-caps line) and `syllabus_points` (pre-1953 "Syllabus by the Court") are flagged derived in `derived_note`.
- [x] **`get_subsequent_history`** — rehearings, remands, supplemental opinions. Mostly surfaces what's already modeled (the §10 supplemental-publication rows). **DONE:** links opinions sharing a `docket_number`, ordered by date with a relation hint; note warns it also catches companions/same-docket dups (verify before citing).
- [x] **`authoring_justice_on_issue`** — what a sitting panel member has previously written on an issue; predictive signal for a bench memo. **DONE:** FTS on the issue filtered to `author LIKE <justice>`, ranked, with snippets; note flags that it reflects authored majority/lead opinions (separate writings best-attributed 1997+).

**Human-initiated research (Westlaw/Lexis-style): ALL BUILT 2026-05-24** — added to `server.py` (tools) + `ndcourts_mcp/research.py` (pure helpers: Westlaw→FTS5 translation, authority normalization, salient-term extraction). User-ratified design choices recorded inline. Verified against the live DB.
- [x] **Boolean/proximity search** — Westlaw-style connectors (`/s` same sentence, `/p` same paragraph, `/N` within N words, `&`, `!`); FTS5 `NEAR` covers most. Judges/appellate lawyers think in these operators. **DONE as `search_boolean`:** `&`→AND, `|`/OR→OR, `%`/NOT→NOT (but-not), `/N`→NEAR/N, `/s`→NEAR/20, `/p`→NEAR/50 (FTS5 has no sentence/paragraph unit — approximations are labeled in a `notes` field and the `translated_query` is returned for transparency), `!`→prefix `*`, `"phrases"`.
- [x] **Faceted search** — filter by date range, authoring justice, case type, disposition (affirmed/reversed/remanded), presence of dissent/concurrence. **DONE as `search_faceted`:** date/author/case_type/disposition/has_dissent/has_concurrence/unanimous facets + optional FTS query. Disposition is backed by the new `opinions.disposition` column (batch `disposition-extract-2026-05-24`, below); dissent/concurrence/unanimity derive from voting data (1997+ only).
- [x] **`find_opinions_construing`** — given an N.D.C.C. section or court rule, return every opinion construing it ("what cases interpret N.D.C.C. § 14-09-06.2?"). *Requires a statutory-cite extraction layer first — meaningful preprocessing, very high payoff.* **DONE — the extraction layer already existed** (`text_citations`, 40,390 statute + 16,642 court_rule rows with clean `normalized` cites + official-source URLs). Forgiving input normalization (bare section, `§`, "chapter", rule aliases like `NDRCrimP`); boundary-safe token match (won't let `1-02-02` match `11-02-02`); returns matched authority(ies) + citing opinions newest-first.
- [x] **`more_like_this` / related opinions** — given a case or fact pattern, return doctrinally similar opinions. **DONE (user-ratified "hybrid"):** blends co-citation overlap (shared `text_citations` authorities) with keyword overlap (salient-term FTS via bm25/`rank`), 50/50; each result reports `shared_authorities` + `keyword_score` so the ranking is explainable.

**Serve both skills:**
- [x] **`detect_overruled_in_draft`** — run every case cited in a draft opinion/memo through the citator and flag any overruled/superseded; an automated proofreading pass. (Depends on the citator above; same caution applies.) **DONE 2026-05-24:** dependency-free case-cite extraction (`research.extract_case_cites`, ND/N.W./N.D. forms — the only cites the citator can act on, so no `jetcite` runtime dep), resolve to corpus, run each through the shared `_scan_treatment` helper (factored out of `check_treatment`). Returns `flagged` (possible-negative/distinguished, with citing context), `clear`, and `unresolved` (foreign/typo cites NOT checked). Same conservative caution: heuristic, sentence-local, never a verdict; absent flag ≠ good-law assurance. **Surfaced a real data bug** (oid 12500 mislabeled *Longtine v. Yeado*; text is *State v. Herrick*, 1997 ND 155) — flagged separately, not fixed in this build.
- [x] **`disposition` extraction** — affirmed/reversed/remanded/dismissed as a queryable field. **DONE 2026-05-24:** new `opinions.disposition` column populated for 7,353 opinions via `memo.extract_disposition` (modern all-caps line); heuristic/derived, NULL where absent. Batch `disposition-extract-2026-05-24` (provenance-logged; `triage/extract_disposition_2026-05-24.py`, `--revert`-able); added to `db.py` schema. See CHANGELOG-data.md.
- [ ] **Pre-1997 lookup by synthetic `[YYYY ND nnn]`** in `lookup_opinion` — already tracked under §10 (synthetic-cite resolution in `lookup_opinion`/`cited_by`); listed here for cross-reference.

**Suggested build order:** cheapest-to-build, highest-certainty wins first — `verify_citation`, `verify_quotation`, `get_parallel_citations`, Boolean/proximity search (all queries/formatting over existing clean data). Then the two that most transform this from retrieval into a research substitute but need an extraction/classification layer: `find_opinions_construing` (statute index) and the citator. Prototype the citator's *retrieval* half early (reuses `get_citing_opinions`); layer treatment classification on after.
